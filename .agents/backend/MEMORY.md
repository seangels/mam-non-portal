# Backend persistent memory

> Snapshot này giúp Backend agent mới tiếp tục công việc qua session khác. Source, test và diff hiện tại vẫn là bằng chứng ưu tiên. Không lưu secret trong file này.

## Trạng thái gần nhất

- Update: 2026-08-29 (`ASH-SYNC-01`). `POST /api/v1/google-sheets/sync-assessments` nhận thêm optional `replaceRecordSnapshots` (`AssessmentRecordSnapshotReplacement`: bool `Name`/`GroupLv1Name`/`GroupLv2Name`/`GroupLv3Name`/`RowIndex` + `IReadOnlyList<AssessmentSheetStatus>? SheetStatuses`, prop `HasAnyField`). Null = hành vi cũ y nguyên. Response `SyncAssessmentsFromGoogleSheetsResponse` thêm `int ReplacedRecordSnapshots` (đổi positional → cập nhật site dựng duy nhất trong `GoogleSheetsService`). Helper thuần mới `AdminPortal.Application.GoogleSheets.AssessmentSnapshotReplacementRules`: `Validate` (không có trường nào / `SheetStatuses` rỗng → `AppValidationException` = 400) và `Apply(records, assessmentByCode, spec, now, actorUserId)` merge từng trường đã chọn từ `Assessment` mới vào `AssessmentRecord.AssessmentSnapshot` khi khác giá trị, chỉ stamp `UpdatedAt`/`UpdatedByUserId` khi thực sự đổi, trả số bản ghi đổi. `SyncAssessmentsAsync`: nếu `replaceRecordSnapshots != null` và `actor.Role` ∉ {SuperAdmin, Admin} → `ForbiddenException` (403); gọi `Validate`; sau bước rebuild catalog + bulk insert, load `dbContext.AssessmentRecords.Include(x => x.AssessmentSheet).Where(status ∈ SheetStatuses)` (tracked), gọi `Apply` với `assessmentByCode` (đã build từ `assessmentToInsert`), `SaveChangesAsync` khi có đổi. Audit `GoogleSheets.AssessmentsSynced` thêm `ReplacedRecordSnapshots` + cờ trường + danh sách status. Không migration (`AssessmentSnapshot` là complex property `.ToJson()`). Files: `GoogleSheets/GoogleSheetsModels.cs`, `GoogleSheets/AssessmentSnapshotReplacementRules.cs` (mới), `GoogleSheets/GoogleSheetsService.cs`, `AssessmentSnapshotReplacementRulesTests.cs` (mới), `api/README.md`, `api/requests.http`. Verification: `dotnet build api/AdminPortal.slnx --no-restore` 0/0; `dotnet test api/tests/AdminPortal.UnitTests --no-restore` 101/101. Integration suite **not run**: luồng `SyncAssessmentsAsync` cần Google Sheets live, `FakeGoogleSheetsService` trong `AdminPortalApiTests` ném `NotImplementedException` cho `SyncAssessmentsAsync` (đã vậy từ trước) nên endpoint không cover được — logic thay thế đã tách helper thuần và test. Không production/IIS/deploy; chưa commit.

- Update: 2026-08-28 (`ASH-GRP-01`, điều chỉnh). Đảo thiết kế: **đã gỡ** `PATCH /api/v1/assessment-sheets/{id}/record-group`, `IAssessmentSheetService.UpdateRecordGroupAsync`, DTO `UpdateAssessmentSheetRecordGroupRequest`, và các helper `AssessmentSheetRules.EnsureCanUpdateRecordGroup`/`NormalizeRecordGroupName`/`ResolveRecordGroupSelection` (+ helper phụ). Thay bằng 2 thứ:
  1. `AssessmentSheetRecordRequest` có thêm `GroupLv2Name`/`GroupLv3Name` (nullable, MaxLength 500). `AssessmentSheetRules.BuildReplacementRecord` chọn tên nhóm snapshot theo: `NormalizeOptional(requestRecord.GroupLvXName)` → `previousSnapshot?.GroupLvXName` → `assessment.GroupLvXName`. UI (ASH-GRP-01) đổi tên nhóm tại chỗ rồi gửi kèm `PUT /assessment-sheets/{id}/records` khi lưu; bỏ trống = giữ hành vi cũ (backward compatible).
  2. `PATCH /api/v1/assessments/group` (catalog-only) — `AssessmentController.UpdateGroup` → `AssessmentService.UpdateGroupAsync`. Payload `{ level: 2|3, assessmentCodes: string[], name }`. Chỉ `Admin|SuperAdmin` (`ForbiddenException` cho Teacher). Code không đủ → `AssessmentNotFound`; code trùng → `SnapshotChanged`. Đặt `Assessment.GroupLv2Name`/`GroupLv3Name` cho tất cả code về `name`; audit `Assessment.GroupUpdated` (EntityType `Assessment`, EntityId `Guid.Empty`, kèm `AssessmentCodes`). `AssessmentService` giờ inject thêm `TimeProvider`. Không đụng snapshot bất kỳ sheet nào. Không migration/schema.
  - Files: `AssessmentModels.cs`, `AssessmentService.cs`, `AssessmentController.cs`, `AssessmentSheetModels.cs`, `AssessmentSheetRules.cs`, `AssessmentSheetService.cs`, `IAssessmentSheetService.cs`, `AssessmentSheetsController.cs`, `AssessmentSheetRulesTests.cs`, `AdminPortalApiTests.cs`, `api/README.md`, `api/requests.http`.
  - Verification: `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 95/95; `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0/0; integration project build pass, **suite not run (Docker not available)** — 2 test viết lại: `AssessmentSheetRecordReplacePersistsRequestProvidedSnapshotGroupNamesWithoutTouchingCatalog`, `AssessmentGroupCatalogPatchUpdatesCatalogForManagersOnlyAndAudits`.

- Update: 2026-08-28 (`ASH-IMP-02`). Import Excel AssessmentSheet nhận thêm 3 header **tùy chọn** (file cũ vẫn chạy): `STT`, `groupLv2Name`, `groupLv3Name`. `AssessmentSheetService` (partial): `ParseExcelImportRows` đọc cột tùy chọn qua `headerMap.TryGetValue`; thêm `ApplyGroupNameFillDown` (fill-down non-empty gần nhất phía trên, độc lập lv2/lv3, reset theo `(NormalizedStudentCode, StartDate, DueDate)`) và `TryParseImportInt` (nhận cả `"1.0"`, sai kiểu → warning, không chặn). `BuildImportedRecord` nhận thêm `displayOrder`/`groupLv2Name`/`groupLv3Name`: ghi `AssessmentRecord.DisplayOrder` là **số chạy toàn cục 1..N theo thứ tự `group.Rows`** (không dùng số STT per-nhóm trong file), snapshot `GroupLv2Name`/`GroupLv3Name` = giá trị file (đã fill-down) ?? `Assessment`; `Code`/`Name`/`GroupLv1Name`/`RowIndex` vẫn theo `Assessment`. `ImportAssessmentSheetsPreviewRowResponse` thêm `Stt`/`GroupLv2Name`/`GroupLv3Name` (giá trị hiệu lực, gồm fallback Assessment). **`AssessmentSheetRules.BuildReplacementRecord` đổi chữ ký**: thêm param `AssessmentSnapshot? previousSnapshot` ngay trước `now`; snapshot `GroupLv2Name`/`GroupLv3Name` = `previousSnapshot?.X ?? assessment.X`. `ReplaceRecordsAsync` build `previousSnapshotByCode` từ `oldRecords` (theo `AssessmentSnapshot.Code`) và truyền vào — nên `PUT /assessment-sheets/{id}/records` không còn xoá tên nhóm snapshot tùy chỉnh (import khcn / `ASH-GRP-01`). Không migration mới (`AssessmentRecord.DisplayOrder` đã có từ `ASH-STT-01`). File: `AssessmentSheetService.cs`, `AssessmentSheetService.ExcelImportTypes.cs`, `AssessmentSheetRules.cs`, `AssessmentSheetModels.cs`, `AssessmentSheetRulesTests.cs`, `AdminPortalApiTests.cs`, `api/README.md`, `api/requests.http`. Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release --no-restore` 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` 94/94; integration test project build 0/0 nhưng **suite chưa chạy** (Docker daemon off — npipe `dockerDesktopLinuxEngine` không kết nối). Không production/IIS, không gọi Google.

- Update: 2026-08-29 (`ASH-SYNC-02`, reverses the 2026-08-28 single-line decision). Per explicit user direction, `Assessment.Name`/`GroupLv1Name`/`GroupLv2Name`/`GroupLv3Name` **must keep newlines from sync all the way to `AssessmentRecord.AssessmentSnapshot`**. `AssessmentSyncTextNormalizer.NormalizeOptionalName` is now just `string.IsNullOrWhiteSpace(value) ? null : value.Trim()` (trim both ends only; inner CRLF/LF, double spaces and blank lines preserved). `NormalizeRequiredName` still returns `string.Empty` for blank. No other write path collapsed newlines: `AssessmentSheetRules.BuildReplacementRecord` copies `assessment.Name`/`GroupLv1Name` verbatim and its `NormalizeOptional` is trim-only; Excel import (`AssessmentSheetService`) uses `.Trim()` only; `AssessmentSnapshotReplacementRules.Apply` (`ASH-SYNC-01`) copies verbatim; `AssessmentService.UpdateGroupAsync` is already `.Trim()`-only. Files: `AssessmentSyncTextNormalizer.cs`, `AssessmentSyncTextNormalizerTests.cs` (rewritten to lock trim-only + newline-preserving behavior), `api/README.md`. Verification: `dotnet build src/AdminPortal.Application/AdminPortal.Application.csproj --no-restore` 0/0; `dotnet test tests/AdminPortal.UnitTests --no-restore` 102/102 (`dotnet build AdminPortal.slnx` only warned MSB3026 copying into `AdminPortal.Api/bin` because a dev API process held the DLL — not a compile error). No schema/migration/contract/authorization/audit change.

- Historical (2026-08-28, superseded by the entry above): a bugfix had `AssessmentSyncTextNormalizer` collapse every whitespace run (CRLF/LF/CR/tab) to one ASCII space to keep synced names single-line. That decision was reversed on 2026-08-29.

- Update: 2026-08-27. Added anonymous local/manual Google credential smoke endpoint `GET /api/v1/google-sheets/credential-smoke`. It calls `IGoogleSheetsService.SmokeTestCredentialAsync`, lazily initializes OAuth credentials through the existing Google Sheets service, performs a metadata-only read of configured `GoogleSheets:SpreadsheetId`, and returns only safe fields (`success`, `isConfigured`, spreadsheet id/title, first sheet title, optional read metadata, coarse `errorCode`). It does not require portal login, does not re-add `[F01]` legacy endpoints, and does not return/log tokens, credential contents, or raw Google exception details. Files: `GoogleSheetsController.cs`, `IGoogleSheetsService.cs`, `GoogleSheetsModels.cs`, `GoogleSheetsService.cs`, `AdminPortalApiTests.cs`, `api/README.md`, `api/requests.http`. Verification: `dotnet build AdminPortal.slnx -c Release --no-restore` pass 0 warnings/errors; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; sandbox integration failed due Docker named pipe `UnauthorizedAccessException`, then `dotnet test tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 31/31 outside sandbox. Live Google OAuth/read smoke was not run in this automated pass.

- Cập nhật: 2026-08-27. `ASH-CL-01` cleanup: AssessmentSheet không còn dùng Google Sheet riêng `[F01]`. Đã gỡ contract/source hiện hành cho `export-to-sheet`, `sync-to-sheet`, `generate-plan-pdf`, `generate-result-pdf`; `AssessmentSheetSpreadsheetId` giữ lại trong entity/DB như legacy-only, không expose DTO/API/UI v1 và không ghi mới. `GoogleSheetsService` chỉ còn các luồng hợp lệ: `sync-assessments` từ `[F0]`, `submit-results` ghi `FinalGrade`/`FinalNote` về `[F0.ĐG]`, upload PDF UI-rendered vào Drive. Các log/memory cũ bên dưới nhắc `[F01]` chỉ là lịch sử, không dùng để triển khai lại. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 30/30 ngoài sandbox.

- Cập nhật: 2026-08-27. Thêm endpoint companion cho UI-rendered Result PDF: `POST /api/v1/assessment-sheets/{id}/upload-result-pdf`, nhận multipart field `file`, validate `.pdf`/không rỗng/tối đa 10 MB giống `upload-plan-pdf`, upload vào `Student.DriveFolderId`, cập nhật `AssessmentSheet.ResultFileLinkPdf`, audit `AssessmentSheet.ResultPdfUploaded`, trả `AssessmentSheetDetail`. Không đổi schema/migration; quyền giữ theo AssessmentSheets hiện tại (`Teacher`/`Admin`/`SuperAdmin`). File chính: `AssessmentSheetsController.cs`, `IAssessmentSheetService.cs`, `AssessmentSheetService.cs`, `IGoogleSheetsService.cs`, `GoogleSheetsService.cs`, `AdminPortalApiTests.cs`, `api/README.md`, `api/requests.http`. Verification: `dotnet build AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; integration trong sandbox fail do Docker named pipe AccessDenied, chạy lại ngoài sandbox `dotnet test tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29. Chưa smoke test Google Drive thật.

- Cập nhật: 2026-08-26. Điều chỉnh quyền `/api/v1/teachers`: `GET /teachers` và `GET /teachers/{id}` mở cho mọi user đã đăng nhập, bao gồm role `Teacher`; mutation `POST`/`PUT`/`DELETE`/`PUT {id}/attendance-policy` vẫn chỉ `Admin`/`SuperAdmin` qua policy `PortalManagers`. Code đổi ở `TeachersController` (class `[Authorize]`, mutation action `[Authorize(Policy = "PortalManagers")]`) và `TeacherService.ListAsync/GetAsync` dùng lại `AuthorizationRules.EnsureReadRole`. Regression test cập nhật để Teacher login đọc được list/detail nhưng `POST`/`PUT`/policy update vẫn 403. `api/README.md` và `docs/plans/03-TCH-teacher-management.md` đã ghi lại contract. Verification: `dotnet build AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29 ngoài sandbox cho Docker/Testcontainers. Có một lần build song song với unit bị lock `sourcelink.json`, rerun tuần tự đã pass.

- Cập nhật: 2026-08-26. Hoàn tất `CQRS-BE-00`: refactor backend theo hướng Mediator + CQRS bằng walking skeleton không đổi REST contract và chưa thêm NuGet MediatR. Đã thêm `AdminPortal.Application.Common.Mediator` (`IAppMediator`, request/query/command marker, handler, `Unit`, `DefaultAppMediator`), tách Users CQRS adapter theo từng file trong `Users/Queries` và `Users/Commands`, đăng ký handler trong `Infrastructure/DependencyInjection`, và đổi `UsersController` gọi mediator cho toàn bộ Users CRUD/password/delete nhưng vẫn giữ `UserService` làm nơi chứa business logic trong bước đầu để giảm rủi ro. Tradeoff đã chọn: mediator nội bộ nhỏ giúp migrate từng endpoint và tránh phụ thuộc package/licensing/version; nếu sau này cần pipeline behavior phong phú/notification/scan assembly chuẩn có thể cân nhắc MediatR hoặc package Mediator riêng ở một phase có benchmark/test. Verification: `dotnet build AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29. Debug/default build chưa pass vì `AdminPortal.Api.exe` đang khóa DLL `bin/Debug`; không tự dừng process. Next: `CQRS-BE-01` migrate Setup/Auth, giữ cookie/CSRF ở controller.

- Cập nhật: 2026-08-26. `GoogleSheetsService.CreateCredential` đổi từ service-account credential (`GoogleCredential.FromStream` + `CreateScoped`) sang OAuth 2.0 tài khoản Google cá nhân qua `GoogleWebAuthorizationBroker.AuthorizeAsync` ("installed app" flow): đọc `GoogleSheets:CredentialFilePath` như OAuth client secret JSON (loại Desktop app, không phải service account key nữa), cache refresh token vào `FileDataStore` tại `GoogleSheets:TokenStorePath` (mặc định thư mục `google-token-store`), định danh local `GoogleSheets:AuthUser` (mặc định `user`). `_credential` field đổi type `Lazy<GoogleCredential>` → `Lazy<UserCredential>`; `CreateSheetsService`/`CreateDriveService`/`ExportSheetToPdfAsync` không đổi vì `UserCredential` vẫn implement `IConfigurableHttpClientInitializer`/`ITokenAccess`. Thêm 2 property `TokenStorePath`/`AuthUser` vào `GoogleSheetsSettings`/`IGoogleSheetsSettings` và `appsettings.json`. **Quan trọng cho vận hành**: lần đầu chạy sau đổi này sẽ cần mở trình duyệt để consent bằng tài khoản Google cá nhân (không chạy được headless/không tương tác cho tới khi có token cache); xoá thư mục `TokenStorePath` nếu cần đổi tài khoản hoặc re-consent. File chính: `GoogleSheetsService.cs`, `IGoogleSheetsService.cs`, `appsettings.json`, `api/README.md`. Verification: build riêng `AdminPortal.Application`/`AdminPortal.Infrastructure` (`dotnet build <csproj> --no-restore`) → 0 Warning/0 Error mỗi project; build toàn `AdminPortal.slnx` không chạy được vì `AdminPortal.Api.exe` đang chạy khoá DLL `bin/Debug` của process khác (không tự kill). Unit/integration test chưa chạy lại trong lượt này — chưa xác minh flow OAuth thật (cần chạy tương tác để test thật, coi là hành động cần xin phép riêng như các lượt gọi Google API thật trước đây).

- Cập nhật: 2026-08-26. Thêm companion endpoint cho ASH-FE-11: `POST /api/v1/assessment-sheets/{id}/upload-plan-pdf`, nhận multipart field `file` (`application/pdf`, tối đa 10 MB), copy bytes vào Drive bằng `IGoogleSheetsService.UploadAssessmentSheetPlanPdfAsync`, cập nhật `AssessmentSheet.PlanFileLinkPdf`, audit `AssessmentSheet.PlanPdfUploaded`, trả `AssessmentSheetDetail`. Luồng này dùng PDF do UI preview/html2pdf render sẵn, không gọi `generate-plan-pdf`, không tạo/ghi `[F01]`, không phụ thuộc mapping Google Sheet cũ. Khi `Student.DriveFolderId` trống thì trả `409 StudentDriveFolderRequired`; lỗi Google thật vẫn đi qua `AssessmentSheetGoogleOperationFailed`. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0/0; `dotnet test api/tests/AdminPortal.UnitTests --no-restore` pass 83/83; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29 ngoài sandbox để truy cập Docker named pipe. Debug build không pass chỉ vì API đang chạy khóa DLL `bin/Debug`.

- Cập nhật: 2026-08-25. `POST /api/v1/assessment-sheets` đổi contract tạo mới từ `assessmentIds[]` sang `records[]` gồm `assessmentId`, `latestGrade`, `note`. `AssessmentSheetService.CreateAsync` validate duplicate/empty theo `records[].assessmentId`, snapshot Assessment từ DB, lưu `latestGrade`/`note` request vào `AssessmentRecord.PlanGrade`/`PlanNote`, giữ `FinalGrade`/`FinalNote` null. ASH `StartDate`/`DueDate` và query date filter được normalize UTC trước khi lưu/query PostgreSQL `timestamptz`. `GoogleSheetsService` credential/Sheets/Drive/HttpClient chuyển sang lazy init để endpoint không dùng Google không fail khi chưa cấu hình credential. File chính: `AssessmentSheetModels.cs`, `AssessmentSheetService.cs`, `AssessmentSheetRules.cs`, `GoogleSheetsService.cs`, `AdminPortalApiTests.cs`, `api/README.md`, `api/requests.http`. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 83/83; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 28/28 sau khi chạy ngoài sandbox để truy cập Docker named pipe.
- Cập nhật: 2026-08-25. `GET /api/v1/assessments` nhận thêm query `studentId` tùy chọn. Khi có `studentId`, response vẫn trả đủ assessment theo filter/sort/paging hiện tại và left join qua `AssessmentSheetLatest` + `AssessmentRecordLatest` để bổ sung nullable `latestGrade`/`latestNote`; `note` tiếp tục là ghi chú gốc của `Assessment`. Teacher chỉ xem latest data cho học sinh thuộc nhóm hiện đang phụ trách; ngoài scope trả `StudentNotFound`/404. File chính: `AssessmentModels.cs`, `AssessmentService.cs`, `AdminPortalApiTests.cs`, `api/README.md`. Verification checkpoint theo yêu cầu không cần DB runtime: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 83/83. Integration test mới cho left join compile qua build Release nhưng không chạy trong chốt cuối.
- Đính chính tài liệu 2026-08-25: các log/memory cũ của ASH nhắc `AssessmentRecordLatest.AssessmentCode` hoặc nói `sync-assessments` chưa ghi `AssessmentSheetLatest`/`AssessmentRecordLatest` đã bị thay thế bởi source hiện tại. Entity cuối dùng `AssessmentId`/`Assessment` FK và unique index (`AssessmentSheetLatestId`, `AssessmentId`); `sync-assessments` hiện đã nạp lại `Assessment`, `AssessmentSheetLatest` và `AssessmentRecordLatest` từ Google Sheet.
- Cập nhật: 2026-08-25. Mở read scope `/api/v1/students` cho `Teacher` để phục vụ UI “Kế hoạch cá nhân”: controller class chỉ yêu cầu authenticated user, còn `POST`/`PUT`/`PUT {id}/group`/`DELETE` vẫn gắn policy `PortalManagers`. `StudentService.ListAsync/GetAsync` cho phép `SuperAdmin`/`Admin` đọc toàn bộ, `Teacher` chỉ đọc học sinh thuộc nhóm mà Teacher hiện phụ trách (`Student.Group.ResponsibleTeacher.UserId == actor.UserId`); direct get ngoài scope trả `StudentNotFound`/404. File chính: `StudentsController.cs`, `StudentService.cs`, `AdminPortalApiTests.cs`, `MigrationUpgradeTests.cs`, `api/README.md`. Verification: `dotnet build api/AdminPortal.slnx --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests --no-restore` pass 83/83; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 26/26 sau khi chạy ngoài sandbox để truy cập Docker named pipe. `MigrationUpgradeTests` giờ migrate tiếp tới latest model trước khi query current entity để bao cột mới như `drive_folder_id`.
- Cập nhật: 2026-08-24. API có thêm phương án opt-in serve SPA từ `src/AdminPortal.Api/ClientApp/build`: `SpaOptions` (`Spa:ServeFromClientAppBuild`, `Spa:BuildPath`), middleware static files + fallback trong `Program.cs`, fallback không bắt `/api`, `/health`, `/openapi`, `/swagger` và không bắt request có extension. `AdminPortal.Api.csproj` copy nội dung `ClientApp/build` vào output/publish nhưng không copy `.gitkeep`; `api/README.md` đã ghi cách bật. Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj --no-restore` pass 0 warning/error; `dotnet test tests/AdminPortal.UnitTests/AdminPortal.UnitTests.csproj --no-restore` pass 83/83. Không chạy UI build, production publish/package hoặc IIS.
- Cập nhật: 2026-08-20 (nhiều lượt trong ngày; xem `docs/tasks/07-ASH/log.md` cho lịch sử chi tiết theo mã task).
- `ASH-BE-00`/`ASH-BE-01` xong (field/settings/EF configuration/migration `20260820094414_AddAssessmentSheetManagement`). `ASH-BE-02` (`AssessmentSheetService`/`AssessmentSheetsController`) đã có đầy đủ CRUD + plan-candidates + status, xuất hiện từ một lượt/agent khác trong cùng ngày (không phải lượt này) — đã đọc lại source thật để xác nhận (không dựa vào memory cũ) trước khi viết mục này.
- Chỉnh sửa đồng thời từ một tiến trình khác trên `AssessmentRecordLatest.cs`/`AssessmentRecordLatestConfiguration.cs` (quan sát được nhiều lần đổi nội dung khác nhau trong vài phút) **đã ổn định**: field định danh cuối cùng là `AssessmentId`/`Assessment` FK (không còn `AssessmentCode`, không có FK `AssessmentSheetId` trỏ `AssessmentSheet`), khớp thiết kế "bảng mirror chỉ-đọc, không gắn với một `AssessmentSheet` cụ thể" đã chốt trong requirements 09. Tiến trình kia tự tạo migration mới `20260820105149_ChangeAssessmentRecordLatest` (đè lên migration `20260820094414_AddAssessmentSheetManagement` do lượt trước tạo) và đã commit ở `01f5cb9 [ASH] update AssessmentRecordLatest`; migration này cũng tự áp lại đúng kiểu fix CA1861 (hoist `columns:` inline array thành `private static readonly string[]`) đã dùng ở migration đầu. Xác nhận bằng build + test thật (dòng dưới), không suy đoán.
- `AssessmentSheetService`: list/detail/plan-candidates/create/update/replace-records/status. Create snapshot Student/Assessment, chặn Student inactive, lưu `PlanGrade`/`PlanNote` từ `records[].latestGrade`/`records[].note` trong request tạo mới (UI lấy từ latest đang hiển thị), để trống `Final*` khi tạo; replace records giữ `Plan*`/`Final*` độc lập; `Done` khoá `Update`/`ReplaceRecords` (`ConflictException` + `ProblemCodes.AssessmentSheetDone`) nhưng **không** khoá `UpdateStatus` — mở lại `Done`→`Open` được bởi mọi vai trò, không cần lý do (đúng yêu cầu). Quyền: `EnsureAssessmentSheetRole` cho phép `Teacher`/`Admin`/`SuperAdmin`, không giới hạn nhóm.
- `POST /google-sheets/sync-assessments`: role check chuyển vào `GoogleSheetsService.EnsureAssessmentSyncRole` (Teacher/Admin/SuperAdmin), không đổi định nghĩa policy `PortalManagers` dùng chung cho API khác — đúng cách đã chốt ở `ASH-DEC-02`. Vẫn chỉ đồng bộ lại `Assessment`; **chưa implement upsert `AssessmentSheetLatest`/`AssessmentRecordLatest`** (grep xác nhận zero call site ghi 2 bảng này ngoài entity definition).
- 5 action `export-to-sheet`/`sync-to-sheet`/`generate-plan-pdf`/`generate-result-pdf`/`submit-results` là stub thật (luôn `throw` `ProblemCodes.AssessmentSheetGoogleMappingBlocked` sau khi check quyền + tồn tại) — **có 2 bản triển khai stub song song, không dùng chung code**: một trong `AssessmentSheetService`/`AssessmentSheetsController`, một trong `GoogleSheetsService`/`GoogleSheetsController`. Cần quyết định gộp về một chỗ trước khi implement thật ở `ASH-BE-03`/`04`, tránh 2 luồng logic riêng cho cùng 5 hành động.
- Khoảng trống đã phát hiện, chưa xử lý: `ProblemCodes.AssessmentSheetAlreadyExists` được khai báo nhưng không nơi nào throw — `Name` trùng trong cùng học sinh (unique index DB) hiện sẽ rơi vào `DbUpdateException` không được bắt riêng ở `CreateAsync`, không trả lỗi thân thiện.
- **`ASH-BE-02` hoàn tất** (2026-08-20, cập nhật contract 2026-08-25 ở `ASH-CR-01`). Đã tách toàn bộ logic thuần khỏi `AssessmentSheetService` vào `AssessmentSheetRules.cs` (static class, theo convention `AuthorizationRules`/`AttendanceRules`/`StudentRules`): `EnsureAssessmentSheetRole`, `EnsureOpen`, `EnsureDistinctIds`, `GradeRank`, `BuildRecords` (lưu `PlanGrade`/`PlanNote` từ request tạo mới), `BuildReplacementRecord` (map request → entity lúc `PUT .../records`, giữ `Plan*`/`Final*` độc lập — có `NormalizeOptional` riêng, cố ý duplicate theo convention repo, không dùng chung với service). `AssessmentSheetRulesTests.cs` bao phủ rule chính của `ASH-BE-02`; integration `ASH-CR-01` xác nhận create persist đúng `PlanGrade`/`PlanNote` từ payload.
- **Đã thử và loại bỏ:** test tích hợp `AssessmentSheetService` bằng EF Core InMemory provider (`AdminPortalDbContext` in-memory, gọi thẳng `CreateAsync`/`ReplaceRecordsAsync`) — phát hiện InMemory **không tương thích** với cách codebase map `AssessmentSnapshot`/`StudentSnapshot` qua `ComplexProperty(...).ToJson()` (jsonb): bất kỳ `OrderBy`/`ThenBy`/`Select`/`ToDictionaryAsync` trên sub-property JSON đều ném `KeyNotFoundException` trong shaper của InMemory, kể cả `BuildDetailAsync` cơ bản nhất (`.ThenBy(x => x.AssessmentSnapshot.Code)`). Đây là giới hạn thật của provider, không phải lỗi test — nhớ đừng thử lại InMemory cho bất kỳ entity nào dùng `ToJson()` complex type trong codebase này (áp dụng cho mọi entity có field jsonb, không riêng ASH). Đã gỡ file test và package `Microsoft.EntityFrameworkCore.InMemory` khỏi `AdminPortal.UnitTests.csproj` — round-trip CRUD qua DB thật chỉ khả thi qua `IntegrationTests` (Postgres thật, đang bị `NU1903` chặn) hoặc smoke test thủ công.
- **`ASH-BE-03`/`ASH-BE-04` — 5 action Google Sheet giờ gọi API thật, không còn stub** (2026-08-20). Trước khi code, phát hiện requirements 09 mục 15 nói rõ mapping cột chi tiết cho sheet `data`/`khcn_template`/`KQ_template` **chưa được cung cấp**; hỏi lại người dùng qua `AskUserQuestion`, người dùng chọn "dùng mapping tạm suy đoán hợp lý, chấp nhận rủi ro sai". Đã implement:
  - Thêm package `Google.Apis.Drive.v3` (1.75.0.4218); credential scope mở rộng từ chỉ `Spreadsheets` sang thêm `DriveService.Scope.Drive`.
  - `GoogleSheetsService.EnsureAssessmentSheetSpreadsheetAsync` — Drive `files.copy` lazy từ `AssessmentSheetTemplateFileId`, set `AssessmentSheetSpreadsheetId` + `SaveChangesAsync` ngay (dùng chung `IApplicationDbContext` scoped instance với `AssessmentSheetService` trong cùng request).
  - `WriteAssessmentSheetDataAsync`/`GenerateAssessmentSheetPlanPdfAsync`/`GenerateAssessmentSheetResultPdfAsync` — ghi sheet `data`/`khcn_template`/`KQ_template` qua helper chung `WriteRecordsToSheetAsync` (Clear rồi Update, idempotent). **TẠM/CHƯA XÁC NHẬN**: định dạng cột là suy đoán của tôi (Mã mục/Tên mục/Nhóm 1-2-3/Kế hoạch/Ghi chú kế hoạch/Kết quả/Ghi chú kết quả), sửa lại `BuildHeaderRow`/`WriteRecordsToSheetAsync` khi có mapping thật.
  - `WriteFinalGradesToSourceSheetAsync` — ghi `[F0.ĐG]`: đọc `ĐG!E16:E1000`/`ĐG!H16:BZ16`, dò vị trí bằng `GoogleSheetsGridLocator` (static, thuần, có unit test: `ColumnIndexToLetter`/`FindAbsoluteRow`/`FindAbsoluteColumnIndex`), ghi nhãn qua `AssessmentSheetRules.GradeLabel` (có unit test, mapping **đã xác nhận thật**, không phải đoán) bằng `BatchUpdate`. Nếu bất kỳ mã nào không dò được vị trí, throw trước khi ghi bất kỳ ô nào (tránh trạng thái nửa vời).
  - PDF: export qua `https://docs.google.com/spreadsheets/d/{id}/export?format=pdf&gid={gid}` với Bearer token từ `((ITokenAccess)_credential).GetAccessTokenForRequestAsync()`, lưu bytes vào **cơ chế file storage mới** (trước đây chưa từng có trong codebase — grep xác nhận không `IFileStorage`/`wwwroot`/`UseStaticFiles` nào tồn tại trước lượt này): `AssessmentSheetFileStorageOptions` (Application layer, section `AssessmentSheetFiles`, mặc định `App_Data/assessment-sheets` + prefix `/files/assessment-sheets`), `Program.cs` thêm `UseStaticFiles` map thư mục này.
  - Gỡ 5 method dead-code trùng lặp trong `GoogleSheetsService`/`IGoogleSheetsService` (phát hiện từ trước, giờ dọn: `ExportAssessmentSheetToSheetAsync` v.v., trả `AssessmentSheetGoogleActionResponse` không route nào gọi) và record đó trong `GoogleSheetsModels.cs`. Thêm `ProblemCodes.AssessmentSheetGoogleOperationFailed` cho lỗi Google/Drive thật.
  - **`ASH-BE-03` mục 4 (upsert `AssessmentSheetLatest`/`AssessmentRecordLatest`) đã hoàn tất sau khi hỏi lại người dùng** cấu trúc cột thật của sheet `_data_DG` (khác `_data_DG_only_item`): header dòng 7, dữ liệu từ dòng 9, cột `ma_hs`=mã học sinh, `item_id`=mã mục đánh giá, `ket_qua`=nhãn kết quả (cùng bảng nhãn đã dùng ở `[F0.ĐG]`). Thêm `GoogleSheetsService.ReadLatestResultsAsync` + `AssessmentSheetRules.TryParseGradeLabel` (chiều ngược `GradeLabel`, có test). **Phát hiện và sửa trước khi nó gây lỗi**: `AssessmentRecordLatest` FK `Restrict` tới cả `Assessment` lẫn `AssessmentSheetLatest` — nếu không đổi thứ tự xoá trong `SyncAssessmentsAsync` (con trước cha: `AssessmentRecordLatest` → `AssessmentSheetLatest`/`Assessment`), lần `sync-assessments` **thứ hai trở đi** sẽ vỡ ràng buộc khoá ngoại (lần đầu không lỗi vì bảng `*Latest` rỗng, dễ bị bỏ sót nếu chỉ test 1 lần). `ASH-BE-03` giờ đủ 4/4 mục.
  - **Vẫn chưa gọi Google API thật lần nào** dù repo có credential thật (`google-credentials.qlmamnon-400006.json`) — gọi thật sẽ tạo file thật trên Drive tài khoản thật/đọc dữ liệu thật, coi là hành động ảnh hưởng hệ thống bên ngoài cần xin phép trước; đã hỏi, người dùng chọn "chưa cần, để sau". `ASH-BE-03`/`ASH-BE-04` do đó **không được đánh `[x]`** trong `status.md` dù code đã viết xong và build/test pass, đúng quy ước "không tick khi chưa chạy kiểm tra tương ứng" — việc còn lại trước khi tick là chạy live một lần để xác nhận, không phải viết thêm code.
  - Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release` → 83/83 pass; `dotnet-ef migrations has-pending-model-changes --configuration Release` → sạch (không đổi entity trong toàn bộ `ASH-BE-03`/`04`, chỉ đổi Application layer; dùng `--configuration Release` vì Debug DLL bị khoá bởi `AdminPortal.Api.exe` PID đang chạy).
- **`ASH-BE-04` đổi cơ chế lưu PDF ngay sau khi bản đầu đã pass build/test** (2026-08-20): người dùng yêu cầu "không lưu file dưới local, tương tác tạo file trên google drive". Gỡ bỏ hoàn toàn cơ chế đĩa cục bộ vừa thêm (`AssessmentSheetFileStorageOptions.cs`, DI, `UseStaticFiles`, section `AssessmentSheetFiles` trong `appsettings.json`) — **đây KHÔNG còn dùng trong codebase, đừng đề xuất lại** nếu thấy nhắc tới trong lịch sử/log cũ. Thay bằng `GoogleSheetsService.SavePdfToDriveAsync` (Drive API v3 `Files.Create`/`Files.Update` multipart upload).
  - Người dùng làm rõ vị trí lưu: **mỗi học sinh có 1 `Student.DriveFolderId`** (field mới, `string?`, migration `20260820163846_AddStudentDriveFolderId`) — **nhập thủ công qua UI quản lý Student, backend chỉ đọc, không tự tạo folder**. Đã lộ field qua `CreateStudentRequest`/`UpdateStudentRequest`/`StudentResponse` và toàn bộ `StudentService.cs` (Create/Update/ChangedFields/audit/2 hàm `Map`/`StudentRow`/`ProjectRows`) — đây là thay đổi trên entity `Student` dùng chung, không riêng ASH, các agent khác cần biết field này tồn tại khi động vào Student CRUD.
  - `[F01]` (copy từ template) và PDF đều đặt `Parents` = `Student.DriveFolderId` nếu có; null thì không set `Parents` (Drive tự chọn vị trí mặc định của service account).
  - Sinh PDF lại: trích id file cũ từ `webViewLink` lưu trong `PlanFileLinkPdf`/`ResultFileLinkPdf` (regex `/d/([a-zA-Z0-9_-]+)`), gọi `Files.Update` đè lên đúng file đó — không tạo file rác trên Drive mỗi lần sinh lại.
  - Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release` → 83/83 pass; `dotnet-ef migrations has-pending-model-changes --configuration Release` → sạch.
- **Hằng số sheet Google chuyển hết vào `appsettings.json`** (2026-08-21): `GoogleSheetsSettings`/`IGoogleSheetsSettings` giờ có thêm `ResultSourceSheetName`/`ResultSourceFirstDataRow`/`ResultSourceFirstStudentColumnIndex` (vị trí `[F0.ĐG]`), `DataSheetName`/`PlanTemplateSheetName`/`PlanTemplateSheetGid`/`ResultTemplateSheetName`/`ResultTemplateSheetGid` (3 sheet trong `[F01]`), `LatestResultsSheetName`/`LatestResultsHeaderRow`/`LatestResultsFirstDataRow` (sheet `_data_DG`) — tất cả có default trùng giá trị cũ nên không đổi hành vi nếu không sửa config. `GoogleSheetsService.cs` không còn `private const` nào cho các giá trị này, chỉ còn field instance đọc từ `IOptions<GoogleSheetsSettings>` trong constructor. Nếu cần đổi tên sheet/gid/vị trí dò ô sau này, sửa `appsettings.json`, không cần build lại code.
- `AdminPortal.IntegrationTests` **vẫn bị chặn restore hoàn toàn** bởi `NU1903` (advisory bảo mật cao trên `SSH.NET 2024.2.0`, transitive qua `Testcontainers.PostgreSql 4.8.1` → `Docker.DotNet.Enhanced.X509`), xác nhận lỗi y hệt khi restore trực tiếp project này (không phải lỗi solution-level). Đã hỏi người dùng có muốn nâng `Testcontainers.PostgreSql` lên 4.14.0 (bản mới nhất trên NuGet, khả năng cao đã vá) hay không — **người dùng chọn hoãn, không nâng cấp trong lượt này**. Không tự ý nâng cấp nếu chưa được yêu cầu lại.
- Verification đã chạy và xác nhận thật (lượt này, sau khi entity ổn định): `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0 Warning/0 Error; `dotnet test tests/AdminPortal.UnitTests/AdminPortal.UnitTests.csproj -c Release` → 40/40 pass (không có test ASH nào trong 40 này, toàn bộ là test cũ). Dùng `-c Release` thay vì Debug vì có `AdminPortal.Api.exe` PID đang chạy khoá DLL `bin/Debug` — không tự kill process của người dùng.

## Bản đồ code

- `api/src/AdminPortal.Domain`: thêm `Teacher`, `StudentGroup`, `AttendanceSheet`, `AttendanceRecord` và attendance/group enum.
- `api/src/AdminPortal.Application`: thêm feature `Teachers`, `StudentGroups`, `Attendance`, business rules và machine-readable `ProblemCodes`; không phụ thuộc EF implementation.
- `api/src/AdminPortal.Infrastructure`: Npgsql/EF Core, mapping/migration, password/JWT, setup transaction và DI.
- `api/src/AdminPortal.Api`: controller, JWT/session validation, CSRF, CORS/rate limit, ProblemDetails, logging, OpenAPI và health.
- `api/tests/AdminPortal.UnitTests`: authorization/validation rules và `VietnameseSearchNormalizerTests.cs`.
- `api/tests/AdminPortal.IntegrationTests`: `WebApplicationFactory` + PostgreSQL Testcontainers; Teacher contract/race/lifecycle ở `TeacherManagementApiTests.cs`, upgrade rehearsal ở `MigrationUpgradeTests.cs`.
- `api/tools/AdminPortal.Maintenance` và `api/scripts/maintenance/cleanup-retention.sql`: cleanup batch audit 90 ngày/session history 30 ngày.
- Contract nền và feature: `docs/plans/01-BASE-admin-portal.md` đến các plan liên quan trong `docs/plans/`; plan có thứ tự tại `docs/plans/README.md`; hướng dẫn chạy/vận hành: `api/README.md`; sample HTTP: `api/requests.http`; tiến độ liên agent: `docs/tasks/README.md` và các thư mục `docs/tasks/<NN-CODE>/`.
- Root `tasks.md` đã được đóng băng dạng legacy; bản backup tham khảo nằm tại `docs/tasks/archive/root-tasks-legacy-2026-08-25.md`. Backend agent không ghi thêm vào root `tasks.md`.

## Contract đang được frontend sử dụng

- API base `/api/v1`; JSON enum là chuỗi; lỗi là `ProblemDetails` có `traceId`.
- Setup anonymous: `GET /setup/status`, `POST /setup/super-admin`; POST chỉ thành công khi chưa từng có user, kể cả record soft-deleted.
- Auth: `POST /auth/login`, `GET /auth/csrf`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me`.
- Login/refresh trả access token, `expiresIn`, `csrfToken`, user. Access token giữ ở memory phía UI; refresh cookie là Secure/HttpOnly/SameSite=None; CSRF gửi qua `X-CSRF-TOKEN`.
- `/users` list/detail/create/full PUT/delete chỉ quản lý role `Admin` và list chỉ dành cho SuperAdmin. Teacher qua User CRUD trả `TeacherMustBeManagedViaTeachers`; ngoại lệ duy nhất là `PUT /users/{userId}/password`, tiếp tục hỗ trợ Teacher.
- Teacher management canonical: `GET /teachers` và `GET /teachers/{id}` cho phép Teacher/Admin/SuperAdmin read-only; `POST`, full `PUT`, policy `PUT` và `DELETE` chỉ dành cho Admin/SuperAdmin. Create tạo User+Teacher atomic; full PUT/policy nhận `expectedVersion`, DELETE nhận query `expectedVersion`; policy 1–7 ngày.
- Teacher list filter `status/groupId/unassigned`, sort whitelist theo plan và search literal mã/tên/email/phone không dấu/case-insensitive tại .NET trên toàn candidate trước total/paging. Không dùng PostgreSQL `unaccent`; blank search giữ DB fast path.
- Teacher response có `id/userId/teacherCode`, field account, policy, group count, timestamps và integer `version`; detail thêm nullable `note` và responsible group summaries. Group assignment chỉ qua StudentGroup endpoint.
- Attendance: GET context/daily; POST sheet full roster; PUT sheet full replacement; Admin/SuperAdmin historical-recovery và ba candidate API. DTO/date/query chính xác nằm trong `docs/plans/02-ATT-attendance.md` section 7.
- Attendance status có `Present|AbsentFullDay|AbsentHalfDay|OneToOneHour|Unmarked`; summary có `rosterTotal/present/absent/oneToOne/unmarked`. Missing vẫn mặc định FullDay thành Present và OneToOne thành OneToOneHour/60, không tự sinh Unmarked.
- Mọi POST mới, kể cả historical recovery, yêu cầu `AbsentHalfDay.halfDayPart=null`, `isExcused` bắt buộc; API notes vẫn tối đa 2.000 ký tự. Saved full PUT giữ `halfDayPart` Morning/Afternoon đang lưu khi record vẫn là AbsentHalfDay, nhưng clear khi đổi status. Wire response và DB column vẫn giữ để đọc lịch sử.
- Student response có `studySchedule { mode, weekdays }` và integer `version`. Mode là `OneToOne|FullDay`; weekday chỉ Monday–Saturday, response canonical. Create/full PUT bắt buộc schedule; full PUT/group/delete nhận `expectedVersion`; stale trả `StudentVersionConflict` + `currentVersion`.
- Student list thêm `studyMode`, `studyWeekday`, sort `studyMode`. Group mutation canonical vẫn là `PUT /students/{id}/group`; DELETE dùng query `expectedVersion`.
- Pagination mặc định page 1/pageSize 20, tối đa 100. User sort whitelist: `email`, `fullName`, `role`, `status`, `createdAt`. Student: `studentCode`, `fullName`, `nickName`, `dateOfBirth`, `gender`, `status`, `studyMode`, `createdAt`.
- `UserStatus`: `Active`, `Inactive`, `Locked`; `StudentStatus`: `Active`, `Inactive`; `Gender`: `Male`, `Female`, `Other` hoặc null.
- Role rule: SuperAdmin quản lý Admin qua `/users` và Teacher qua `/teachers`; Admin chỉ Teacher mutation qua `/teachers`; User list không trả SuperAdmin/Teacher. Teacher không có quyền mutation API quản trị, nhưng được read-only `/teachers` list/detail và read-only `/students` theo scope nhóm đang phụ trách để chọn học sinh cho nghiệp vụ giáo viên.

## Bất biến security và persistence

- Access token mặc định 15 phút có `sid`; JWT validation truy vấn `auth_sessions` mỗi bearer request để revoke tức thì. Refresh token 30 ngày, rotate khi refresh, DB chỉ lưu hash.
- Thay role/status/password hoặc soft-delete user phải revoke mọi session active. Logout có thể chạy không bearer bằng refresh cookie + CSRF.
- Password policy: tối thiểu 12 ký tự, có chữ hoa, chữ thường, số và ký tự đặc biệt. Login lockout sau 5 lần sai trong 15 phút. Không tiết lộ lý do auth chi tiết cho client.
- Setup dùng PostgreSQL transaction advisory lock `SetupService` và `IgnoreQueryFilters()` để chống race/mở lại setup sau soft delete.
- User/Student dùng global soft-delete query filter. Unique normalized email và student code là partial unique index trên `deleted_at IS NULL`; student code có thể tái sử dụng sau delete.
- Student schedule lưu `study_mode` + `study_weekday_mask` 1..63; bit Monday=1 đến Saturday=32, không expose mask. Student `version` là integer concurrency token; migration legacy backfill FullDay/mask63/version1 cho cả soft-deleted row.
- Teacher row không hard-delete; `teacher_code` trim/uppercase/unique trên mọi Teacher row nên mã của Teacher đã xóa vẫn được giữ. Legacy profile được backfill `GV-MIG-{UUID}`. Full PUT/policy/delete tăng aggregate `version`; password và group assignment không tăng Teacher version.
- Lock order Teacher -> User -> group UUID tăng dần. Rename fullName tăng snapshot version đúng một cho mọi group vẫn đang phụ trách trong cùng transaction; code/email/phone/note/policy không tăng group snapshot. StudentGroup assignment lock Teacher trước Group để không đảo lock order.
- Mọi snapshot mutation lock group row (nhiều group theo UUID tăng dần) và tăng `snapshot_version`; create sheet cũng lock group. PUT sheet lock row và dùng `expectedVersion`; conflict trả `ProblemDetails.code` cùng version hiện tại khi có.
- Missing daily không ghi DB và lọc Student active theo group + weekday mask. FullDay mặc định Present; OneToOne mặc định OneToOneHour/60. Không có scheduled Student trả `NoScheduledStudents` và cấm empty sheet. Saved sheet giữ full snapshot, không re-filter khi schedule thay đổi; historical recovery vẫn explicit/manual.
- Student full PUT lock/check Student version trước business validation, rồi lock group; thay identity attendance hoặc schedule tăng group snapshot đúng một, profile-only/no-op không tăng snapshot. Assignment thật tăng Student version + snapshot old/new; same-group/current-version là no-op. Student lock dùng PostgreSQL `FOR NO KEY UPDATE`: vẫn serialize writers nhưng tương thích AttendanceRecord FK `KEY SHARE`, tránh deadlock khi attendance giữ Group và lưu record.
- Ngày nghiệp vụ mặc định `Asia/Ho_Chi_Minh`; Teacher dùng policy riêng, Admin/SuperAdmin thao tác mọi ngày không tương lai. Recovery chỉ dành manager khi standard historical snapshot không khả dụng.
- Teacher audit chỉ lưu IDs, teacherCode, status/presence flags, changed fields và version transition; không lưu raw fullName/email/phone/note/password. Request logger không log body. Cleanup chạy bên ngoài API, theo batch và an toàn khi chạy lại.
- CORS chỉ dùng allow-list origin cụ thể với credentials, không wildcard. Cookie cross-site yêu cầu HTTPS cho cả API/UI.

## Những lỗi đã từng phát hiện — không tái tạo

- `AddDbContext` phải resolve `IConfiguration` lazily trong factory; nếu đọc connection string khi registration, integration test override sẽ bị bỏ qua.
- JWT bearer phải configure qua DI/`IOptions<JwtOptions>` lazily; capture option rỗng sớm từng gây `IDX10703` trong test.
- Validation attribute trên positional request record phải dùng target `[param: ...]` với ASP.NET Core 10.
- Không dùng `.Select(x => Map(x))` khi `Map` đọc navigation. EF chỉ cho client evaluation ở top-level projection nên navigation không được đưa vào SQL, từng làm `StudentGroup.responsibleTeacherName/studentCount` và Student list `groupCode/groupName` bị null/0. Dùng inline/IQueryable projection để EF translate join/count.
- Không dùng `FOR UPDATE` cho Student row trong workflow SCH: Attendance create giữ Group rồi INSERT FK cần `KEY SHARE` Student; đồng thời schedule PUT giữ Student rồi chờ Group sẽ deadlock 40P01. `FOR NO KEY UPDATE` là lock đúng vì Student mutations không đổi PK/hard-delete và vẫn serialize concurrent Student writers.
- `AuthSession` cần query filter tương thích soft-delete navigation `User`; bỏ nó sẽ tạo warning required-navigation và có thể làm lệch semantics session.
- Middleware request logging phải bọc exception handler đúng thứ tự để log status đã map (ví dụ setup conflict là 409, không phải 200).
- Không sửa/xóa EF Designer hoặc `AdminPortalDbContextModelSnapshot.cs`. Migration hiện có: `20260811000000_InitialCreate`, `20260811130802_AddAttendanceFoundation`, `20260811150730_AddTeacherManagement`, `20260811172348_AddStudentStudySchedule`, `20260811201427_AddAttendanceUnmarkedStatus`. Migration AUI chỉ thay check constraint, không drop column hay rewrite dữ liệu; constraint mới chấp nhận Unmarked với conditional fields null và giữ legacy half-day part.
- EF tooling hiện cảnh báo required navigation trỏ tới entity có soft-delete query filter (`Teacher/User`, attendance `Student/User`). Đây là chủ ý: FK lịch sử vẫn non-null + RESTRICT; các query recovery/history dùng `IgnoreQueryFilters`. Pending-model check vẫn sạch.

## Build, test và migration nhanh

Chạy trong `api/`:

```powershell
dotnet restore AdminPortal.slnx
dotnet build AdminPortal.slnx --no-restore
dotnet test tests/AdminPortal.UnitTests --no-restore
dotnet test tests/AdminPortal.IntegrationTests --no-restore
dotnet tool restore
dotnet tool run dotnet-ef migrations has-pending-model-changes `
  --project src/AdminPortal.Infrastructure `
  --startup-project src/AdminPortal.Api
```

Integration test cần Docker engine vì dùng PostgreSQL thật. EF CLI cần process environment có `Jwt__SigningKey` tạm tối thiểu 32 ký tự; không ghi giá trị vào file/handoff.

## Deploy/IIS snapshot

- Build diễn ra trên máy source; máy IIS khác chỉ nhận package publish.
- Host cố định hiện tại: API `https://api-gv-portal.local`, UI `https://gv-portal.local`; UI origin phải khớp chính xác `Security__AllowedOrigins__0`.
- IIS target hiện tại: `C:\inetpub\api-gv-portal.local` và `C:\inetpub\gv-portal.local`, hai app pool/SNI binding riêng. API cần .NET 10 Hosting Bundle; PostgreSQL 17 chạy riêng.
- Script nguồn: `deploy/iis/build-iis-package.ps1`, `deploy/iis/deploy-iis.ps1`; hướng dẫn: `deploy/iis/HUONG-DAN-DEPLOY-IIS.md`.
- Package đã xác minh tại snapshot: `release/gv-portal-iis-20260811-102752.zip`, SHA-256 `93EA758E0DCD542FDE73A8659A9D4BC96E3C5BA51381AA60010255B9056866F1`; không chứa source, secret, PDB hoặc `appsettings.Development.json`.
- Package trên chỉ là artifact của baseline 2026-08-11. Sau bất kỳ runtime change nào phải build package mới, kiểm tra checksum/nội dung và cập nhật mục này; không deploy nhầm artifact cũ. Không chạy `-Build` trên máy IIS đích.

## Protocol cập nhật và handoff

Khi kết thúc một task backend, cập nhật ngắn gọn file này:

- Ngày và mục tiêu vừa xử lý.
- Contract/quyết định/bất biến mới; xóa hoặc sửa thông tin đã lỗi thời.
- File chính đã đổi.
- Command test/build/migration đã thực sự chạy, configuration và kết quả chính xác.
- Blocker hoặc việc tiếp theo, kèm owner nếu cần frontend/root/deploy phối hợp.
- Nếu sinh package mới, ghi tên file và SHA-256 sau khi đã verify.

Không biến memory thành nhật ký dài theo từng thao tác. Giữ “trạng thái hiện tại + lý do quan trọng + next step”. Tuyệt đối không ghi password, JWT key, token, connection string có credential, private key/certificate secret hoặc giá trị secret từ environment.

## Cập nhật gần nhất

- 2026-08-28 `ASH-STT-01` (branch `minor/assessment-sheet-stt-edit`, CHƯA build/test theo yêu cầu user "chỉ code"): thêm cột `int? DisplayOrder` cho `AssessmentRecord` = STT hiển thị người dùng chỉnh/lưu trên form, tách biệt `AssessmentRowIndex`. Migration tay `20260828120000_AddAssessmentRecordDisplayOrder` (+ Designer + snapshot) chỉ add cột nullable `display_order`, chưa chạy `dotnet-ef ... has-pending-model-changes` để đối chiếu. `AssessmentSheetRecordRequest` nhận `DisplayOrder` (`Range(0,int.MaxValue)`, default null); `AssessmentSheetRules.BuildReplacementRecord` gán `DisplayOrder = requestRecord.DisplayOrder` và giữ `AssessmentRowIndex = assessment.RowIndex`. `AssessmentSheetRecordResponse` thêm `int? DisplayOrder`; `BuildDetailAsync` project `x.DisplayOrder`. Thứ tự GET đổi thành `OrderBy(DisplayOrder ?? int.MaxValue).ThenBy(AssessmentRowIndex ?? int.MaxValue).ThenBy(Code)` ở cả `BuildDetailAsync` và `LoadRecordEntitiesAsync` để giữ thứ tự người dùng sắp. UI khi sắp lại gửi dãy `displayOrder` liên tục `1..N`. Docs: `api/README.md`, `api/requests.http`. NEXT: chạy default backend build/unit/integration + `dotnet-ef migrations has-pending-model-changes` để xác nhận migration tay khớp model.
- 2026-08-28 `ASH-IMP-01`: added AssessmentSheet Excel import using `ExcelDataReader` in `AdminPortal.Application`. New endpoints are `POST /api/v1/assessment-sheets/import-excel/preview` (validate only, returns all non-empty rows + summary) and `POST /api/v1/assessment-sheets/import-excel` (validates again, then creates/updates sheets and replaces records). Import groups by `studentCode + startDate + dueDate`, blocks `Done` sheets, stores file `planGrade`/`planNote` into `PlanGrade`/`PlanNote`, keeps `FinalGrade`/`FinalNote` null, skips duplicate record rows with warnings, and writes metadata audit only. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warnings/errors; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 32/32 outside sandbox. No Google live smoke and no production/IIS/deploy.
- 2026-08-27 `ASH-CR-02`/`ASH-BE-06`: `GoogleSheetsService.WriteFinalGradesToSourceSheetAsync` now returns the ResultSource cells actually written after comparing current Google Sheet values. It syncs both `FinalGrade` and `FinalNote`; the note cell is the column immediately right of the student result column and must have a blank student-code/header cell. `AssessmentSheetService.SubmitResultsAsync` updates `SubmissionDate`, writes a summary audit and one `AssessmentSheet.ResultSourceCellUpdated` audit per changed cell. `SyncAssessmentsAsync` writes `GoogleSheets.AssessmentsSynced`; plan/result PDF uploads log file/link metadata only. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 30/30 outside sandbox after sandbox Docker pipe access was denied. Real Google Sheet/Drive smoke was not run.
