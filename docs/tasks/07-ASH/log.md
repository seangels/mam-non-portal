# Lịch sử thực hiện — ASH: Bảng đánh giá năng lực

File trạng thái: [`status.md`](status.md). Chi tiết từng task (Mục đích/Nội dung/DoD): [`details/`](details/). File log này là nơi ghi lịch sử chung, theo trình tự thời gian thật, cho toàn bộ task của epic — không tách log theo từng file chi tiết.

## Quy ước ghi log

- Mỗi mục là một dòng bullet, gắn đúng mã task (`ASH-P-01`, `ASH-BE-00`…): `- <Mã> (YYYY-MM-DD): <việc đã làm, kết quả, lệnh kiểm tra và số liệu pass/fail>`.
- Ghi theo thời điểm thực hiện thật (không phải ngày lập plan); nối thêm bullet mới ở cuối section tương ứng, không sửa/xoá log cũ — nếu một quyết định trước đó bị đảo ngược, thêm bullet mới ghi rõ thay đổi thay vì chỉnh sửa bullet cũ.
- Khi một task chuyển trạng thái trong `status.md`, phải có ít nhất một bullet log tương ứng ở đây cùng ngày.
- Không ghi secret, connection string có credential, JWT signing key hay token vào file này (theo `AGENTS.md`).

## Planning log

- `ASH-P-01` (2026-08-20): thực hiện đối chiếu source theo checklist. Phát hiện `AssessmentRecord.cs` đã đổi so với lần đối chiếu trước trong cùng ngày: không còn field `Grade` đơn, thay bằng 4 field `PlanGrade`/`PlanNote`/`FinalGrade`/`FinalNote` (khôi phục đúng thiết kế "hai cặp tách biệt" ban đầu). `AssessmentSheet.cs`, `AssessmentSheetLatest.cs`, `AssessmentRecordLatest.cs` không đổi thêm so với lần đối chiếu trước (`AssessmentSheetSpreadsheetId` vẫn chưa có trên `AssessmentSheet` — đúng như dự kiến, đây là việc của `ASH-BE-00`). Đã cập nhật lại toàn bộ requirements 09, sơ đồ luồng dữ liệu (Mermaid + SVG re-render), plan 07-ASH và 13 file `details/`/`status.md` cho khớp field mới. 5 quyết định `ASH-DEC-01..05` giữ nguyên đề xuất mặc định trong plan mục 13 (không có phản hồi khác từ người dùng ngoài các input Google Sheet đã cung cấp) — coi như đã khoá cho `ASH-BE-00` triển khai. `ASH-P-01` hoàn tất.
- `ASH-CL-01` (2026-08-27): bắt đầu cleanup theo yêu cầu mới: AssessmentSheet không còn dùng luồng tạo/copy Google Sheet riêng `[F01]`. Tạo detail `details/22-ASH-CL-01.md`; cập nhật `requirements`, sơ đồ Mermaid/SVG, plan 07-ASH, `api/README.md`, `api/requests.http` và `status.md` để đánh dấu `AssessmentSheetSpreadsheetId`/`export-to-sheet`/`sync-to-sheet`/`generate-plan-pdf`/`generate-result-pdf` là legacy/removed. Chưa chốt `[x]` cho tới khi source build/test pass.
- `ASH-CL-01` (2026-08-27): hoàn tất cleanup code/docs. Source search chỉ còn `AssessmentSheetSpreadsheetId` trong entity + migrations/model snapshot legacy; không còn route/method/config legacy trong source hiện hành. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 30/30 ngoài sandbox; `npm --prefix ui run test:ci` pass 118/118 ngoài sandbox; `npm --prefix ui run build -- --configuration development` pass hash `4c19eda4cb0e62265a4c`. Không gọi Google API thật, không chạy production/IIS.
- `ASH-IMP-01` (2026-08-28): bắt đầu task import `AssessmentSheet`/`AssessmentRecord` từ Excel mẫu `docs/samples/import_khcn.xlsx`. Đã khóa hướng backend dùng thư viện `ExcelDataReader`; format v1 gồm header `assessmentCode`, `studentCode`, `studentName`, `startDate`, `dueDate`; gom sheet theo `studentCode + startDate + dueDate`; duplicate record giữ dòng đầu và trả warning; lỗi bắt buộc sẽ rollback toàn bộ import. Chưa chạy verification.
- `ASH-IMP-01` (2026-08-28): cập nhật yêu cầu UI/contract: sau khi chọn file phải gọi preview validate trước, mở popup `dxDataGrid` hiển thị từng dòng trong file kèm lỗi/warning và summary; chỉ khi file hợp lệ user mới bấm submit để gọi endpoint ghi DB. Backend submit vẫn validate lại file trước khi ghi để tránh preview cũ hoặc file bị đổi. Chưa chạy verification.
- `ASH-IMP-01` (2026-08-28): bổ sung yêu cầu preview trả toàn bộ dòng import đã parse/validate lên client; popup `dxDataGrid` dùng dữ liệu local này để filter/search/sort client-side, không gọi server paging/filter riêng trong bước validate. Chưa chạy verification.
- `ASH-IMP-01` (2026-08-28): người dùng cập nhật file mẫu có thêm `planGrade`/`planNote` và chốt import hai cột này vào `PlanGrade`/`PlanNote`; `FinalGrade`/`FinalNote` vẫn để trống. Preview UI dùng đúng object `summary` từ backend; response submit chuẩn hóa tên field rõ `createdSheetCount`/`updatedSheetCount`/`importedRecordCount`/`skippedDuplicateRowCount`; action hiển thị theo giá trị backend (`Created`, `Updated`, `Invalid`, `SkippedDuplicate`). Các thay đổi manual ở PDF preview được giữ nguyên.
- `ASH-IMP-01` (2026-08-28): hoàn tất code và automated verification. Backend thêm `ExcelDataReader` và hai endpoint `POST /assessment-sheets/import-excel/preview` + `POST /assessment-sheets/import-excel`; preview trả toàn bộ dòng non-empty đã validate cho UI datagrid, submit validate lại rồi tạo/cập nhật sheet theo `studentCode + startDate + dueDate`, thay records khi sheet chưa `Done`, lưu `planGrade`/`planNote` vào `PlanGrade`/`PlanNote`, giữ `FinalGrade`/`FinalNote` null, audit metadata import và không gọi Google/[F01]. Frontend thêm nút `Nhập Excel`, popup `dxDataGrid` local filter/search/sort, summary theo `summary` của BE và nút `Nhập dữ liệu` chỉ bật khi `canImport=true`. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 32/32 ngoài sandbox; `npm --prefix ui run test:ci` pass 120/120 ngoài sandbox; `npm --prefix ui run build -- --configuration development` pass hash `9a5625874ee832852b84`, chỉ warning CommonJS/DevExtreme/html2pdf/canvg quen thuộc. Chưa smoke browser thủ công với file mẫu thật; không chạy production/IIS/deploy.
- `ASH-IMP-02` (2026-08-28): người dùng cập nhật `docs/samples/import_khcn.xlsx` thêm 3 cột `STT`, `groupLv2Name`, `groupLv3Name`. Chốt: `STT` → `AssessmentRecord.DisplayOrder` (số chạy toàn cục `1..N` theo thứ tự dòng, không dùng con số per-nhóm trong file; STT sai kiểu chỉ warning); `groupLv2Name`/`groupLv3Name` fill-down kiểu ô merge (non-empty gần nhất phía trên, độc lập từng cột, reset theo cụm `studentCode+startDate+dueDate`) → `AssessmentSnapshot`, trống hẳn thì fallback `Assessment`.
- `ASH-IMP-02` (2026-08-28): code xong. Backend (`AssessmentSheetService`): `ParseExcelImportRows` đọc 3 header tùy chọn + `ApplyGroupNameFillDown` + `TryParseImportInt`; `BuildImportedRecord` nhận `displayOrder`/`groupLv2Name`/`groupLv3Name`, ghi snapshot ưu tiên giá trị file; `ImportExcelAsync` gán `DisplayOrder` chạy số theo `group.Rows`. `ReplaceRecordsAsync` + `AssessmentSheetRules.BuildReplacementRecord` thêm tham số `previousSnapshot`, map theo `Assessment.Code` để giữ `GroupLv2Name`/`GroupLv3Name` snapshot cũ (đồng thời giải quyết yêu cầu "giữ snapshot tùy chỉnh" của `ASH-GRP-01`). Preview DTO `ImportAssessmentSheetsPreviewRowResponse` thêm `Stt`/`GroupLv2Name`/`GroupLv3Name`. Frontend: `AssessmentSheetImportExcelPreviewRow` + 3 cột popup `STT`/`Nhóm lớn`/`Nhóm nhỏ`; form đã nhóm/tô màu/sắp theo snapshot từ trước (thêm test khóa hành vi). Verification: `dotnet build api/src/AdminPortal.Api/AdminPortal.Api.csproj -c Release --no-restore` 0/0; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` 94/94; integration test project build 0/0 nhưng **chưa chạy suite** vì Docker daemon chưa bật (npipe dockerDesktopLinuxEngine không kết nối được). Frontend `test:ci` + `build --configuration development` **chưa chạy**: ổ đĩa C: đầy (~7 MB trống, ENOSPC ở temp). Không production/IIS/deploy, không gọi Google.

## Backend log

- `ASH-BE-00` (2026-08-20): thêm field `AssessmentSheet.AssessmentSheetSpreadsheetId` (`string?`); xoá `AssessmentSheetLatest.ClosedDate` (`ASH-DEC-03`); thêm `AssessmentSheetTemplateFileId` vào `IGoogleSheetsSettings`/`GoogleSheetsSettings`/`appsettings.json` với giá trị thật `12ClFCOFCfUJJ1i8QstHweNaSdLfY-1MB2eaCuigqWwQ` (`ASH-DEC-04`). Build `AdminPortal.Domain`/`AdminPortal.Application`: 0 warning/0 error. `ASH-BE-00` hoàn tất.
- `ASH-BE-01` (2026-08-20): thêm `DbSet<AssessmentSheet>`/`AssessmentRecord`/`AssessmentSheetLatest`/`AssessmentRecordLatest` vào `IApplicationDbContext` và `AdminPortalDbContext`; tạo 4 file `IEntityTypeConfiguration` mới (`AssessmentSheetConfiguration`, `AssessmentRecordConfiguration`, `AssessmentSheetLatestConfiguration`, `AssessmentRecordLatestConfiguration`) dùng `ComplexProperty(...).ToJson()` cho `StudentSnapshot`/`AssessmentSnapshot` (map jsonb), `HasConversion<string>()` cho enum, `DeleteBehavior.Restrict` cho mọi FK (khớp quy ước hiện có, không cascade).
  - **Phát hiện khi chạy `dotnet-ef migrations add`:** EF Core từ chối `HasIndex(x => new { x.AssessmentSheetLatestId, x.AssessmentSnapshot.Code })` — lỗi "not a valid member access expression" vì không hỗ trợ index trên sub-property của complex type đã map JSON. Xử lý: thêm field scalar `AssessmentRecordLatest.AssessmentCode` (`string`, required, `maxLength 50`) làm cột index riêng, độc lập với `AssessmentSnapshot` jsonb. Đã cập nhật requirements 09 và plan 07-ASH mục 5/6.5/13 để ghi nhận field mới này và nhắc `ASH-BE-03` phải set nó khi upsert.
  - Sinh migration `20260820094414_AddAssessmentSheetManagement` (tạo 4 bảng `assessment_sheets`, `assessment_records`, `assessment_sheet_latests`, `assessment_record_latests`, đúng unique index theo `ASH-DEC-05`, không có cột `closed_date`).
  - Migration ban đầu bị chặn bởi phân tích tĩnh `CA1861` (2 lệnh `CreateIndex` composite dùng `new[] { ... }` inline, `TreatWarningsAsErrors=true`) — đã sửa bằng cách hoist thành `private static readonly string[]` field ngay trong file migration (không đổi hành vi SQL sinh ra).
  - Kết quả kiểm tra: `dotnet build` cho `Domain`/`Application`/`Infrastructure`/`Api`: 0 warning/0 error. `dotnet-ef migrations has-pending-model-changes`: "No changes have been made to the model since the last migration." `dotnet test tests/AdminPortal.UnitTests`: 40/40 pass (test cũ, chưa có test riêng cho entity mới — thuộc `ASH-BE-02`/`05`). `dotnet build` toàn `AdminPortal.slnx` (gồm `IntegrationTests`) **bị chặn** bởi lỗi restore NuGet tiền tồn tại không liên quan (`NU1903`: advisory bảo mật trên gói `SSH.NET` 2024.2.0, transitive qua Testcontainers) — không phải do thay đổi của epic này; chưa xử lý, cần agent khác/quyết định riêng về nâng cấp dependency.
  - `ASH-BE-01` hoàn tất.
- `ASH-BE-02`/`ASH-BE-03`/`ASH-BE-04` partial (2026-08-20): tạo `AssessmentSheets` application surface (`IAssessmentSheetService`, DTO, `AssessmentSheetService`) và `AssessmentSheetsController` với list/detail/plan-candidates/create/update/replace-records/status + các route action `export-to-sheet`, `sync-to-sheet`, `generate-plan-pdf`, `generate-result-pdf`, `submit-results`. BE-02 đã có logic CRUD lõi: mọi role `Teacher`/`Admin`/`SuperAdmin` được thao tác không giới hạn nhóm; create snapshot Student/Assessment, chặn Student inactive, prefill `PlanGrade` từ `AssessmentRecordLatest.LatestGrade`, để trống `Final*`; replace records giữ `Plan*` và `Final*` độc lập; `Done` khoá update/records và `Open` mở lại. Đăng ký DI `IAssessmentSheetService`.
  - BE-03 đã làm phần có thể làm trước: mở rộng `IGoogleSheetsService` bằng các method action cho F01/F0.ĐG/PDF; bỏ policy `PortalManagers` khỏi `POST /google-sheets/sync-assessments` và chuyển sang role-check `Teacher`/`Admin`/`SuperAdmin` trong handler để không mở nhầm policy quản trị khác. Các action F01/F0.ĐG hiện trả `ProblemCodes.AssessmentSheetGoogleMappingBlocked` với thông điệp blocker vì chưa có mapping cột/vị trí sheet `data`, dò ô F0.ĐG và Drive copy implementation.
  - BE-04 đã làm phần interface/endpoint trước: route sinh F02/F03 tồn tại và trả blocker rõ (`AssessmentSheetGoogleMappingBlocked`) vì chưa có mapping template `khcn_template`/`KQ_template`, export PDF và cấu hình nơi lưu file.
  - Verification: `dotnet build api/src/AdminPortal.Api/AdminPortal.Api.csproj -c Release --no-restore` pass 0 warning/0 error; `dotnet test api/tests/AdminPortal.UnitTests/AdminPortal.UnitTests.csproj --no-restore` pass 40/40. `dotnet build api/AdminPortal.slnx --no-restore` và integration test vẫn bị chặn bởi `NU1903` package `SSH.NET` 2024.2.0 (transitive Testcontainers) như log BE-01; Debug build API riêng còn bị khóa DLL bởi process dotnet đang chạy nên dùng Release build để xác minh compile.
- `ASH-BE-05` phần 1 (2026-08-20): trong lúc BE-02/03/04 còn dở, phát hiện một tiến trình khác (agent song song) đang chỉnh sửa đồng thời `AssessmentRecordLatest.cs`/`AssessmentRecordLatestConfiguration.cs` — tạm dừng, chỉ làm phần tài liệu không đụng entity theo yêu cầu người dùng (chờ ổn định). Đã cập nhật `api/README.md` (mục "Bảng đánh giá năng lực") và `api/requests.http` (12 route `assessment-sheets` + `google-sheets/sync-assessments`, ghi chú 5 action Google Sheet đang blocked). Về NuGet `NU1903`: đã đề xuất nâng `Testcontainers.PostgreSql` lên 4.14.0 (bản mới nhất, khả năng đã vá) — người dùng chọn hoãn, không nâng cấp trong lượt này.
- `ASH-BE-02` hoàn tất (2026-08-20): tách toàn bộ logic thuần khỏi `AssessmentSheetService` vào `AssessmentSheetRules.cs`, thêm `BuildRecords` (khởi tạo `PlanGrade` từ `LatestGrade`) và `BuildReplacementRecord` (giữ `Plan*`/`Final*` độc lập lúc `PUT .../records`) — trước đó 2 hàm này nằm private trong service, không test độc lập được. `AssessmentSheetRulesTests.cs` nâng lên 19 test case, bao phủ đủ 4 rule DoD yêu cầu (prefill `PlanGrade`, độc lập `Plan*`/`Final*`, khoá `Done`, quyền không giới hạn nhóm). Thử thêm test tích hợp bằng EF Core InMemory (gọi thẳng `AssessmentSheetService` qua `AdminPortalDbContext` in-memory) nhưng phát hiện InMemory provider không tương thích với `ComplexProperty(...).ToJson()` (jsonb) — mọi truy vấn `OrderBy`/`ThenBy`/`Select` trên sub-property JSON đều lỗi `KeyNotFoundException` trong shaper, kể cả `BuildDetailAsync` cơ bản. Đây là giới hạn thật của InMemory provider, không phải bug test — đã gỡ file test và package `Microsoft.EntityFrameworkCore.InMemory` khỏi `AdminPortal.UnitTests.csproj`. `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0 Warning/0 Error; `dotnet test tests/AdminPortal.UnitTests -c Release` → 59/59 pass.
- `ASH-BE-05` phần 2 (2026-08-20): xác nhận chỉnh sửa đồng thời ở trên đã ổn định — entity cuối cùng dùng `AssessmentId`/`Assessment` FK (không phải `AssessmentCode` do `ASH-BE-01` thêm trước đó), tiến trình kia đã tự tạo và commit migration `20260820105149_ChangeAssessmentRecordLatest` (`01f5cb9 [ASH] update AssessmentRecordLatest`), kể cả áp lại đúng fix CA1861 kiểu hoist mảng. Xác minh bằng `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0 Warning/0 Error, `dotnet test tests/AdminPortal.UnitTests -c Release` → 40/40 pass (chưa có test ASH). Sau đó tách `AssessmentSheetRules` (static class theo convention `AuthorizationRules`/`AttendanceRules`/`StudentRules`, gồm `EnsureAssessmentSheetRole`/`EnsureOpen`/`EnsureDistinctIds`/`GradeRank`) ra khỏi `AssessmentSheetService`, viết `AssessmentSheetRulesTests.cs` (15 test case). `dotnet test tests/AdminPortal.UnitTests -c Release` → 55/55 pass. Cập nhật `.agents/backend/MEMORY.md` phản ánh trạng thái xác minh thật (không dựa vào memory cũ), gồm 2 phát hiện chưa xử lý: (1) 5 action Google (export/sync/generate PDF/submit) có 2 bản stub song song không dùng chung giữa `AssessmentSheetService` và `GoogleSheetsService`, cần gộp trước khi implement thật ở `ASH-BE-03`/`04`; (2) `ProblemCodes.AssessmentSheetAlreadyExists` khai báo nhưng không nơi nào throw, tên trùng học sinh sẽ rơi vào `DbUpdateException` thô. `dotnet test tests/AdminPortal.IntegrationTests` vẫn bị chặn bởi `NU1903` như đã ghi ở phần 1 — chưa xử lý, đúng quyết định hoãn của người dùng. `ASH-BE-05` còn thiếu: test tích hợp cho `AssessmentSheetService` (cần EF InMemory/Testcontainers), bước smoke 1/2/3/5/7/9 qua `requests.http` với server thật, và `dotnet-ef migrations has-pending-model-changes` xác nhận lại.

- `ASH-BE-03`/`ASH-BE-04` (2026-08-20): người dùng yêu cầu "hoàn thiện tất cả các task ASH-BE còn lại". Trước khi code, phát hiện requirements 09 mục 15 ghi rõ mapping cột chi tiết cho sheet `data`/`khcn_template`/`KQ_template` **chưa được cung cấp** ("sẽ bổ sung sau... người dùng sẽ chủ động cung cấp") — hỏi lại người dùng cách xử lý qua `AskUserQuestion`, người dùng chọn "Dùng mapping tạm suy đoán hợp lý" (chấp nhận rủi ro sai, sẽ sửa khi có mapping thật).
  - Thêm package `Google.Apis.Drive.v3` (1.75.0.4218) vào `AdminPortal.Application.csproj`; mở rộng credential scope từ chỉ `Spreadsheets` sang thêm `DriveService.Scope.Drive`.
  - Gỡ 5 method dead-code trùng lặp trong `GoogleSheetsService`/`IGoogleSheetsService` (`ExportAssessmentSheetToSheetAsync` v.v., trả về `AssessmentSheetGoogleActionResponse` không route nào gọi tới — phát hiện từ lượt trước, giờ dọn luôn) và `AssessmentSheetGoogleActionResponse` trong `GoogleSheetsModels.cs`.
  - Thêm `IGoogleSheetsService.EnsureAssessmentSheetSpreadsheetAsync`/`WriteAssessmentSheetDataAsync`/`GenerateAssessmentSheetPlanPdfAsync`/`GenerateAssessmentSheetResultPdfAsync`/`WriteFinalGradesToSourceSheetAsync` (real implementation trong `GoogleSheetsService.cs`), inject `IGoogleSheetsService` vào `AssessmentSheetService` (constructor param mới), viết lại 5 method action (`ExportToSheetAsync`/`SyncToSheetAsync`/`GeneratePlanPdfAsync`/`GenerateResultPdfAsync`/`SubmitResultsAsync`) gọi thật thay vì throw blocked.
  - Thêm `GoogleSheetsGridLocator` (static, thuần, có test) cho logic dò ô `[F0.ĐG]` (`ColumnIndexToLetter`, `FindAbsoluteRow`, `FindAbsoluteColumnIndex`) và `AssessmentSheetRules.GradeLabel` (mapping nhãn `A→Đạt +`/`B→Chưa đạt -`/`C→Hỗ trợ +`/`D→Hỗ trợ -`, có test).
  - Thêm cơ chế lưu file PDF cục bộ (trước đây chưa tồn tại trong codebase): `AssessmentSheetFileStorageOptions` (Application layer, section `AssessmentSheetFiles` trong `appsettings.json`, mặc định thư mục `App_Data/assessment-sheets`, path prefix `/files/assessment-sheets`), `Program.cs` thêm `app.UseStaticFiles(...)` map thư mục này.
  - Thêm `ProblemCodes.AssessmentSheetGoogleOperationFailed` cho lỗi Google/Drive thật (phân biệt với `AssessmentSheetGoogleMappingBlocked` cũ — code cũ vẫn giữ trong `ProblemCodes.cs` nhưng không còn nơi nào throw).
  - **Chưa làm — `ASH-BE-03` mục 4** (upsert `AssessmentSheetLatest`/`AssessmentRecordLatest` từ `[F0.data_DG]`): khác với mapping ghi sheet (được người dùng đồng ý cho suy đoán), đây là hướng **đọc** dữ liệu nguồn — sai sót ở đây làm hỏng dữ liệu prefill âm thầm trong DB, không như ghi nhầm cột trên Sheet vẫn còn nhìn thấy để sửa. Cố ý không tự suy đoán mà không hỏi lại; `SyncAssessmentsAsync` giữ nguyên như cũ (chỉ thay `Assessment`).
  - **Chưa gọi Google API thật lần nào**: repo có sẵn credential thật (`google-credentials.qlmamnon-400006.json`) và có thể gọi live được, nhưng việc gọi thật sẽ tạo file thật trên Drive của tài khoản thật — coi là hành động ảnh hưởng hệ thống bên ngoài, cần xin phép người dùng trước, chưa tự ý chạy trong lượt này.
  - Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0 Warning/0 Error; `dotnet test tests/AdminPortal.UnitTests -c Release` → 78/78 pass (thêm `GoogleSheetsGridLocatorTests` 8 case + `GradeLabel` 4 case); `dotnet-ef migrations has-pending-model-changes --configuration Release` → "No changes have been made to the model since the last migration." (không đổi entity ở lượt này).

- `ASH-BE-03` mục 4 hoàn tất (2026-08-20): người dùng cung cấp cấu trúc cột thật của `_data_DG` (khác sheet `_data_DG_only_item` dùng để đồng bộ `Assessment`): header dòng 7, dữ liệu từ dòng 9, cột `ma_hs` = mã học sinh, `item_id` = mã mục đánh giá, `ket_qua` = nhãn kết quả (cùng 4 nhãn `Đạt +`/`Chưa đạt -`/`Hỗ trợ +`/`Hỗ trợ -` đã dùng ở `[F0.ĐG]`).
  - Thêm `GoogleSheetsService.ReadLatestResultsAsync` (đọc `_data_DG` theo tên header, tái dùng `ReadHeaderMappingsAsync` sẵn có) và `AssessmentSheetRules.TryParseGradeLabel` (chiều ngược `GradeLabel`, có unit test).
  - Mở rộng `SyncAssessmentsAsync` để upsert (thực ra là xoá toàn bộ rồi ghi lại — đúng "ghi đè hoàn toàn" theo requirements 09 mục 12) `AssessmentSheetLatest` (theo `StudentId`) và `AssessmentRecordLatest` (theo `AssessmentSheetLatestId`+`AssessmentId`) cùng lúc với `Assessment`.
  - **Phát hiện lỗi tiềm ẩn trước khi nó xảy ra**: `AssessmentRecordLatest` có FK `Restrict` tới `Assessment` và `AssessmentSheetLatest`; nếu chỉ thêm insert mà không đổi thứ tự xoá, `ExecuteDeleteAsync` trên `Assessment` sẽ vỡ ràng buộc khoá ngoại kể từ lần `sync-assessments` **thứ hai** trở đi (lần đầu không lỗi vì bảng `*Latest` rỗng). Đã sửa: xoá `AssessmentRecordLatest` trước, rồi `AssessmentSheetLatest`/`Assessment`, rồi insert lại theo thứ tự cha trước con.
  - Học sinh/mục đánh giá/nhãn không khớp dữ liệu hiện có thì bỏ qua dòng đó (best-effort theo từng dòng, không làm fail cả lượt đồng bộ nhiều học sinh).
  - `ASH-BE-03` giờ đủ 4/4 mục theo DoD; chỉ còn thiếu bước chạy live thật để xác nhận (chưa gọi Google API thật lần nào trong toàn bộ epic này) nên chưa đánh `[x]`.
  - Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release` → 83/83 pass; `dotnet-ef migrations has-pending-model-changes --configuration Release` → sạch.

- `ASH-BE-04` đổi cơ chế lưu PDF (2026-08-20): người dùng yêu cầu "không lưu file dưới local, tương tác tạo file trên google drive" ngay sau khi bản đầu (lưu `App_Data/assessment-sheets/` + `UseStaticFiles`) đã build/test pass. Đã gỡ bỏ toàn bộ cơ chế đĩa cục bộ (`AssessmentSheetFileStorageOptions.cs`, đăng ký DI, `UseStaticFiles` trong `Program.cs`, section `AssessmentSheetFiles` trong `appsettings.json`) và thay bằng `GoogleSheetsService.SavePdfToDriveAsync` (Drive API v3 `Files.Create`/`Files.Update` multipart upload).
  - Người dùng làm rõ thêm: "mỗi student sẽ có folder drive id để chứa các file", sau đó: "folder id sẽ được manual nhập vào ở ui quản lý student" — nghĩa là KHÔNG tự động tạo folder ở backend, chỉ đọc field đã có sẵn.
  - Thêm `Student.DriveFolderId` (`string?`, `HasMaxLength(200)`) — field mới trên entity `Student` (không riêng ASH, ảnh hưởng CRUD Student chung), migration `20260820163846_AddStudentDriveFolderId`. Đã lộ field này qua `CreateStudentRequest`/`UpdateStudentRequest`/`StudentResponse` (`StudentModels.cs`) và cập nhật đủ `StudentService.cs` (Create/Update/ChangedFields/audit/2 hàm Map/StudentRow/ProjectRows) để CRUD Student hiện có hoạt động đúng với field mới.
  - `EnsureAssessmentSheetSpreadsheetAsync` (copy `[F01]`) và `SavePdfToDriveAsync` (tạo/cập nhật PDF) đều đọc `Student.DriveFolderId` qua `GetStudentDriveFolderIdAsync` (chỉ đọc, không ghi) để set `Parents` khi tạo file trên Drive; nếu học sinh chưa có `DriveFolderId`, không set `Parents` (Drive API tự chọn vị trí mặc định).
  - Sinh PDF lại: trích id file cũ từ `webViewLink` đã lưu (regex `/d/([a-zA-Z0-9_-]+)`), gọi `Files.Update` đè nội dung lên đúng file đó thay vì tạo file mới — giữ đúng yêu cầu "chỉ giữ bản mới nhất, không tích luỹ file rác".
  - Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release` → 83/83; `dotnet-ef migrations has-pending-model-changes --configuration Release` → sạch.

- `ASH-BE-03`/`ASH-BE-04` chuyển hằng số sheet ra `appsettings.json` (2026-08-21): người dùng yêu cầu đưa các giá trị đang hardcode trong `GoogleSheetsService.cs` (`ResultSourceSheetName`/`ResultSourceFirstDataRow`/`ResultSourceFirstStudentColumnIndex`, `DataSheetName`/`PlanTemplateSheetName`/`PlanTemplateSheetGid`/`ResultTemplateSheetName`/`ResultTemplateSheetGid`, `LatestResultsSheetName`/`LatestResultsHeaderRow`/`LatestResultsFirstDataRow`) ra cấu hình thay vì `private const`. Đã thêm các property tương ứng vào `GoogleSheetsSettings`/`IGoogleSheetsSettings` (giá trị mặc định giữ đúng như hằng số cũ), đọc vào field instance trong constructor `GoogleSheetsService`, thay mọi usage từ hằng số sang field. Thêm section tương ứng vào `appsettings.json` (mục `GoogleSheets`). Verification: `dotnet build src/AdminPortal.Api/AdminPortal.Api.csproj -c Release` → 0/0; `dotnet test tests/AdminPortal.UnitTests -c Release` → 83/83; `dotnet-ef migrations has-pending-model-changes --configuration Release` → sạch (không đổi entity).

## Frontend log

- `ASH-FE-13` (2026-08-28): bắt đầu chỉnh UI theo yêu cầu mới: chỉ tự động fill `PlanGrade`/`PlanNote` như hiện tại; không tự động fill `FinalGrade`/`FinalNote`. Phạm vi dự kiến chỉ ở frontend mapping/template/test, không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-13` (2026-08-28): hoàn tất code UI. `PlanGrade`/`PlanNote` vẫn được fill từ latest khi tạo/thêm mục; `FinalGrade`/`FinalNote` không còn fallback từ plan/latest khi mở detail, thêm record, serialize dirty state hoặc lưu full-replace records. Cột `Kết quả hiện tại` của `dx-select-box` chỉ bind `finalGrade`; trạng thái chưa nhập hiển thị trống và không bị tô vàng như khác kế hoạch. Verification: sandbox `npm --prefix ui run test:ci` fail `EPERM lstat C:\Users\sangn`, chạy ngoài sandbox pass 118/118; `npm --prefix ui run build -- --configuration development` pass hash `75afc3b9148aa5cfc68c`, chỉ warning CommonJS/DevExtreme/html2pdf/canvg quen thuộc. Không đổi backend/REST contract; không chạy Google live, production/IIS/deploy.

- `ASH-FE-05` (2026-08-25): thêm DevExtreme TagBox lọc `latestGrade` ở đầu hàng filter của màn chọn mục đánh giá. Options hiển thị tiếng Việt: `Chưa có`, `Đạt +`, `Chưa đạt -`, `Hỗ trợ +`, `Hỗ trợ -`; `Chưa có` lọc item có `latestGrade` null/empty. Filter này chạy local trên dữ liệu client cache và trên source hiện hành của `viewMode` giống text/nhóm tuổi/nhóm 2/nhóm 3; ở mode `Chỉ những mục đã chọn`, filter chạy trên snapshot đã chụp và không gọi API khi đổi filter/chọn-bỏ chọn. Verification: `npm --prefix ui run test:ci` pass 92/92; `npm --prefix ui run build -- --configuration development` pass hash `5d6d04404cbb4a850f85`, chỉ còn warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-CR-01` (2026-08-25): đổi contract tạo mới `AssessmentSheet` để UI gửi `records: [{ assessmentId, latestGrade, note }]` thay vì chỉ `assessmentIds[]`; backend lưu `latestGrade`/`note` request vào `AssessmentRecord.PlanGrade`/`PlanNote`, còn `FinalGrade`/`FinalNote` vẫn null. UI picker thêm method lấy selected assessment details từ cache để submit đúng dữ liệu đang hiển thị. Trong lúc thêm integration test, phát hiện `GoogleSheetsService` mở credential ngay trong constructor làm `POST /assessment-sheets` 500 dù create không dùng Google; đã chuyển Google credential/Sheets/Drive/HttpClient sang lazy init. Cũng phát hiện `StartDate`/`DueDate` offset `+07:00` gây lỗi Npgsql `timestamptz`; đã normalize DateTimeOffset đầu vào của ASH sang UTC trước khi lưu/filter. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0/0; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 83/83; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 28/28 sau khi chạy ngoài sandbox để truy cập Docker named pipe; `npm --prefix ui run test:ci` pass 94/94; `npm --prefix ui run build -- --configuration development` pass hash `8a507d8bc67d4ea098ab` với warning CommonJS DevExtreme quen thuộc. Không chạy production/IIS/deploy.
- `ASH-FE-06` (2026-08-25): sửa lỗi màn edit không đổi được `AssessmentSheet.status`. Nguyên nhân: `statusEditorOptions.readOnly` từng được tính theo `isCreate` trước khi route mode được set; bản sửa tạm bằng getter làm option object đổi reference liên tục, không phù hợp DevExtreme 19. Đã khôi phục `statusEditorOptions` thành object ổn định, set `readOnly` trong `ngOnInit` sau khi biết mode: create readonly, edit editable. Thêm Jasmine regression test cho create/edit status readonly. Verification: `npm --prefix ui run test:ci` pass 95/95; `npm --prefix ui run build -- --configuration development` pass hash `3839b780a34f23d577c3` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-07` (2026-08-25): bắt đầu triển khai theo yêu cầu mới: màn edit có nút `Thêm mục đánh giá`, mở lại `app-assessment-picker` ở chế độ thêm từng dòng; dòng đã có record vẫn xuất hiện nhưng không cho thêm lại; khi thêm phải confirm và lưu qua endpoint full-replace records hiện có. Ghi proposal vào [`details/14-ASH-FE-07.md`](details/14-ASH-FE-07.md) trước khi sửa code.
- `ASH-FE-07` (2026-08-25): hoàn thành frontend add-mode cho màn edit AssessmentSheet. `app-assessment-picker` có mode mặc định `select` cho màn create và mode `add` cho màn edit; mode `add` ẩn checkbox/select-all/view-mode, thêm cột `Thao tác` với nút `Thêm`, và hiển thị `Đã có` cho assessment code đã tồn tại trong sheet. Form edit có nút `Thêm mục đánh giá`; thao tác thêm hỏi confirm, build request full-replace từ records hiện có + assessment mới, map record cũ theo `assessment.code` qua cache picker và chặn nếu không map được để tránh mất dữ liệu. Thêm DTO/method `replaceRecords` ở frontend service, không đổi REST contract backend. Verification: sandbox npm bị `EPERM lstat C:\Users\sangn`, chạy ngoài sandbox `npm --prefix ui run test:ci` pass 98/98; `npm --prefix ui run build -- --configuration development` pass hash `598ddedccbdd234ed7c0` với warning CommonJS DevExtreme quen thuộc. Không production/IIS/deploy.
- `ASH-FE-07` (2026-08-25): chỉnh UI theo feedback: nút thêm từng dòng trong picker edit bỏ chữ `Thêm`, chỉ giữ icon `+` và tooltip/hint để bảng gọn hơn. Chưa chạy verification tại thời điểm ghi log này.
- `ASH-FE-07` (2026-08-25): xác minh lại sau chỉnh icon-only: `npm --prefix ui run test:ci` pass 98/98; `npm --prefix ui run build -- --configuration development` pass hash `94d1d5e4e212cd3cbcfe` với warning CommonJS DevExtreme quen thuộc. Không production/IIS/deploy.
- `ASH-FE-08` (2026-08-25): bắt đầu triển khai nút xóa từng dòng trong `records-panel` của màn edit AssessmentSheet. Cơ chế dự kiến: icon xóa từng record, confirm trước khi xóa, lưu qua endpoint full-replace records hiện có, giữ nguyên dữ liệu các dòng còn lại và chặn xóa dòng cuối cùng do backend yêu cầu ít nhất một record.
- `ASH-FE-08` (2026-08-25): hoàn thành nút icon xóa từng dòng trong `records-panel` của màn edit AssessmentSheet. Khi xóa sẽ confirm, chặn sheet `Done`, chặn xóa dòng cuối cùng, build request full-replace từ các dòng còn lại và giữ nguyên `planGrade`/`planNote`/`finalGrade`/`finalNote`; nếu không map được record còn lại sang assessment hiện hành thì báo lỗi để tránh mất dữ liệu. Verification: `npm --prefix ui run test:ci` pass 100/100; `npm --prefix ui run build -- --configuration development` pass hash `ee8959c55c2b7f77969d` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): tạo task detail trước theo yêu cầu người dùng để đổi `records-panel` từ card sang table giống mẫu: cột xóa nằm đầu, các cột nhóm `groupLv2`/`groupLv3`, `STT`, `Nội dung đánh giá`, `Kết quả hiện tại`, `Ghi chú`; màu nền cố định theo 5 nhóm `groupLv2`. Chưa sửa source UI, chưa chạy verification.
- `ASH-FE-09` (2026-08-25): bắt đầu thực thi đổi `records-panel` sang table. Phạm vi giữ ở frontend UI, không đổi backend/REST contract và không chạy production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): hoàn thành phần code đổi `records-panel` sang table thường thay cho card. Table có cột xóa đầu dòng, group `groupLv2`/`groupLv3` dùng rowspan theo nhóm liên tiếp, `STT` reset theo từng `groupLv3`, màu nền cố định theo 5 nhóm `groupLv2` bằng normalize tiếng Việt, cột `Kết quả hiện tại` bind `finalGrade`, cột `Ghi chú` bind `finalNote` và save qua endpoint full-replace records hiện có khi bấm `Lưu thay đổi`. Verification automated: `npm --prefix ui run test:ci` pass 103/103; `npm --prefix ui run build -- --configuration development` pass hash `cefc36fec652cb3d9215` với warning CommonJS DevExtreme quen thuộc. Chưa chạy smoke thủ công trên màn edit thật, nên task vẫn giữ `[~]` thay vì `[x]`. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: chữ trong cột `Nhóm lớn`/`Nhóm nhỏ` đổi từ xoay dọc sang ngang bình thường để giảm chiều cao mỗi nhóm; tăng nhẹ width hai cột nhóm. Verification: `npm --prefix ui run build -- --configuration development` pass hash `2c64824c002c6b59fb99` với warning CommonJS DevExtreme quen thuộc. Không chạy lại `test:ci` vì thay đổi chỉ là CSS; test `103/103` đã pass ngay trước đó trong cùng task. Không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: chuyển cột icon xóa từ đầu bảng sang sau cột `Ghi chú` trong `records-panel`. Verification: `npm --prefix ui run build -- --configuration development` pass hash `4cc87a609392ac1c7f7c` với warning CommonJS DevExtreme quen thuộc. Không chạy lại `test:ci` vì thay đổi chỉ là thứ tự cột template; test `103/103` đã pass trước đó trong cùng task. Không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: giảm font-size của `records-panel` và `assessment-picker` xuống `small`; riêng cell `Nhóm lớn`/`Nhóm nhỏ` có rowspan 1 dòng dùng `smaller` để gọn hơn. Verification: `npm --prefix ui run build -- --configuration development` pass hash `972a493a50e127fe3171` với warning CommonJS DevExtreme quen thuộc. Không chạy lại `test:ci` vì thay đổi chỉ là CSS/class binding nhỏ; test `103/103` đã pass trước đó trong cùng task. Không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: đưa `planGrade` ra cột riêng `Kế hoạch` nằm trước `Kết quả hiện tại`; header `Kết quả hiện tại` có checkbox `Hiện kế hoạch` để bật/ẩn cột này. Verification: `npm --prefix ui run build -- --configuration development` pass hash `fa42c338e158461cc696` với warning CommonJS DevExtreme quen thuộc. Không chạy lại `test:ci` vì thay đổi chỉ là template/state hiển thị nhỏ; test `103/103` đã pass trước đó trong cùng task. Không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback để giảm diện tích mỗi hàng: tách `assessment.code` ra cột riêng `Mã` nằm trước `Nội dung đánh giá`, header `Nội dung đánh giá` có checkbox `Hiện mã` để bật/ẩn cột này, cột nội dung chỉ còn tên assessment, và textarea `Ghi chú` mặc định còn 1 hàng. Verification: `npm --prefix ui run build -- --configuration development` pass hash `e9a78f5ad0e9cc96b74c` với warning CommonJS DevExtreme quen thuộc. Không chạy lại `test:ci` vì thay đổi chỉ là template/CSS/state hiển thị nhỏ; test `103/103` đã pass trước đó trong cùng task. Không production/IIS/deploy.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: thêm `ASSESSMENT_GROUP_LV2_DISPLAY_ORDER` và `ASSESSMENT_GROUP_LV2_COLORS` vào `core/models/api.models.assessment-sheets` để tái sử dụng; `records-table` sort stable theo thứ tự nhóm lớn: `Phát triển thể chất`, `Phát triển nhận thức`, `Phát triển ngôn ngữ`, `Cá nhân và xã hội`, `Tiền tiểu học`; nhóm ngoài cấu hình đứng sau và giữ thứ tự tương đối; màu nhóm cũng lấy từ shared config thay vì local config trong form. Verification: `npm --prefix ui run test:ci` pass 104/104 và `npm --prefix ui run build -- --configuration development` pass hash `628573dc041cbd8cc418` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy; manual browser smoke vẫn chưa chạy nên `ASH-FE-09` giữ `[~]`.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: gộp 2 shared constants `ASSESSMENT_GROUP_LV2_DISPLAY_ORDER` và `ASSESSMENT_GROUP_LV2_COLORS` thành một const `ASSESSMENT_GROUP_LV2_CONFIGS` dạng array object `{ key, displayOrder, bgcolor }`; `records-table` vẫn sort theo `displayOrder` và lấy màu từ `bgcolor`. Verification: `npm --prefix ui run test:ci` pass 104/104 và `npm --prefix ui run build -- --configuration development` pass hash `98006d39e8b6b264f4b0` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy; manual browser smoke vẫn chưa chạy nên `ASH-FE-09` giữ `[~]`.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: cột `Kế hoạch` hiển thị `planGrade` dạng badge; cột `Kết quả hiện tại` đổi từ native select sang `dx-select-box`; cả hai lấy text/màu chữ/màu nền từ `ASSESSMENT_GRADE_OPTIONS`, giá trị chưa có dùng pill trung tính. Verification: `npm --prefix ui run test:ci` pass 105/105 và `npm --prefix ui run build -- --configuration development` pass hash `9d9972fed0152f86c372` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy; manual browser smoke vẫn chưa chạy nên `ASH-FE-09` giữ `[~]`.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: `Kết quả hiện tại` được fill khởi tạo bằng `Kế hoạch`: khi load records hoặc build request add/save/xóa, `finalGrade` trống sẽ fallback sang `planGrade`, còn `finalGrade` đã có thì giữ nguyên. Verification: `npm --prefix ui run test:ci` pass 106/106 và `npm --prefix ui run build -- --configuration development` pass hash `c6413847c32bea22e0c9` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy; manual browser smoke vẫn chưa chạy nên `ASH-FE-09` giữ `[~]`.
- `ASH-FE-09` (2026-08-25): chỉnh theo feedback: cột `Kế hoạch` đổi sang `dx-button` dạng badge màu nhưng không tương tác; `Kết quả hiện tại` giữ `dx-select-box` và chỉ style màu nền/chữ của selected value theo grade đang chọn, bỏ custom item-template badge trong dropdown. Verification: `npm --prefix ui run test:ci` pass 106/106 và `npm --prefix ui run build -- --configuration development` pass hash `e4d2ffe010f1b06f9963` với warning CommonJS DevExtreme quen thuộc. Không đổi backend/REST contract, không production/IIS/deploy; manual browser smoke vẫn chưa chạy nên `ASH-FE-09` giữ `[~]`.
- `ASH-FE-10` (2026-08-26): tạo task detail theo yêu cầu: đổi `Ghi chú` mỗi dòng sang `dxTextArea`; sheet `Open` vẫn cho đổi giáo viên phụ trách, thêm/xóa Assessment Record và chỉnh record; khi trạng thái đang chọn/load là `Planed` thì khóa chỉnh sửa trên UI, BE giữ logic hiện tại. Chưa sửa source, chưa chạy verification.
- `ASH-FE-10` (2026-08-26): cập nhật lại rule theo phản hồi người dùng: `Planed` chỉ khóa thêm/xóa Assessment Record, vẫn cho nhập/lưu `FinalGrade` và `FinalNote` (và không khóa đổi giáo viên phụ trách nếu form có quyền); `Done` giữ khóa như hiện tại. Chưa sửa source, chưa chạy verification.
- `ASH-FE-10` (2026-08-26): bắt đầu thực thi frontend. Phạm vi giữ UI-only: đổi note record sang `dxTextArea`, gom helper khóa thao tác theo trạng thái, dùng `editor.status` hiện tại để khóa thêm/xóa ngay khi chọn `Planed`; không đổi backend/REST contract và không chạy production/IIS/deploy.
- `ASH-FE-10` (2026-08-26): hoàn thành code UI. `records-panel` dùng DevExtreme `dxTextArea` cho `FinalNote`; add/remove record dựa trên trạng thái đang chọn trong form (`Open` cho phép, `Planed`/`Done` khóa), còn `FinalGrade`/`FinalNote` chỉ khóa khi `Done`. Thêm Jasmine coverage cho helper và component state. Verification: sandbox npm bị `EPERM lstat C:\Users\sangn`, chạy ngoài sandbox `npm --prefix ui run test:ci` pass 109/109; `npm --prefix ui run build -- --configuration development` pass hash `2cdd3e9076b4c5566eb0` với warning CommonJS DevExtreme quen thuộc. Chưa chạy smoke thủ công màn edit nên `ASH-FE-10` giữ `[~]`; không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-11` (2026-08-26): tạo task detail mới để thêm nút `In Kế hoạch PDF` trên màn edit AssessmentSheet. Yêu cầu: dùng `html2pdf.js` phía frontend dựa trên mẫu `docs/samples/khcn-standalone.html`, render PDF từ dữ liệu kế hoạch hiện tại (`PlanGrade`/`PlanNote`), không gọi backend `generate-plan-pdf`, không cập nhật `PlanFileLinkPdf`, không đổi REST contract/backend/auth/routing/IIS. Chưa sửa source UI, chưa chạy verification.
- `ASH-FE-11` (2026-08-26): cập nhật task theo yêu cầu mới. `In Kế hoạch PDF` từ màn edit sẽ mở trang preview HTML trước; preview có nút quay lại edit, nút mở blob PDF bằng `html2pdf().outputPdf('bloburl')`, và nút tạo PDF vào thư mục Google Drive của học viên. Đợt/tên PDF tính từ `startDate`/`dueDate` theo dạng ví dụ `3 tháng 6.7.8.26`; độ tuổi tính từ ngày sinh tới `startDate`. Vì lưu Drive từ PDF do UI render cần backend/service account nếu chưa có endpoint upload, task đã ghi rõ khả năng cần companion backend/contract. Chưa sửa source, chưa chạy verification.
- `ASH-FE-11` (2026-08-26): bổ sung yêu cầu cấu hình `html2canvas.letterRendering: true` trong `html2pdf` để ưu tiên vẽ chữ chính xác cao khi render PDF. Chỉ cập nhật task detail, chưa sửa source, chưa chạy verification.
- `ASH-FE-11` (2026-08-26): bắt đầu thực thi task. Phạm vi: thêm dependency `html2pdf.js@0.10.1`, route/trang preview kế hoạch PDF, nút `In Kế hoạch PDF` từ màn edit, helper mapping dữ liệu in và endpoint riêng upload PDF do UI tạo lên Drive; theo yêu cầu người dùng, lần này không kiểm tra lưu file PDF thật trên Google Drive.
- `ASH-FE-11` (2026-08-26): hoàn tất code và automated verification. Frontend thêm preview route `/#/assessment-sheets/:id/plan-pdf-preview`, nút `In Kế hoạch PDF`, helper tính đợt/tuổi/filename, render bảng A4 từ `PlanGrade`/`PlanNote`, `Mở PDF` bằng `html2pdf().outputPdf('bloburl')`, và nút upload PDF lên Drive. Backend thêm `POST /api/v1/assessment-sheets/{id}/upload-plan-pdf` nhận multipart PDF tối đa 10 MB, upload vào `Student.DriveFolderId`, cập nhật `PlanFileLinkPdf`, trả `409 StudentDriveFolderRequired` khi thiếu folder id. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass; `dotnet test api/tests/AdminPortal.UnitTests --no-restore` pass 83/83; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29 khi chạy ngoài sandbox để truy cập Docker named pipe; `npm --prefix ui run test:ci` pass 114/114; `npm --prefix ui run build -- --configuration development` pass. `dotnet build api/AdminPortal.slnx --no-restore` Debug không pass do API đang chạy khóa DLL trong `bin/Debug`, không phải lỗi compile. Smoke thủ công và kiểm tra lưu file PDF Google Drive thật chưa chạy theo đúng yêu cầu chừa lại, nên `ASH-FE-11` vẫn giữ `[~]`.
- `ASH-FE-11` (2026-08-26, commit `25fe8e4`): đổi `AssessmentSheetPlanPreviewComponent` từ mô hình phân trang nhiều trang A4 sang ép luôn 1 trang, tự co scale nội dung theo cơ chế của `docs/samples/khcn-standalone.html` (`fitContentToPage()` đo `.pdf-content.scrollHeight` so với `.pdf-page.clientHeight`, chỉ scale xuống, không scale lên; chạy ở `ngAfterViewChecked`, `window:resize`, và đầu `createPdfWorker()`). Đánh đổi hiển thị: sheet nhiều dòng sẽ bị co nhỏ lại thay vì sang trang 2. Cùng lúc đổi filename PDF từ prefix `ke-hoach-ca-nhan-` sang `khcn-` trong `buildPlanPdfFileName` (`assessment-sheet-plan-preview.models.ts`) nhưng **không cập nhật lại 2 unit test cũ** kỳ vọng prefix `ke-hoach-ca-nhan-` — phát hiện lại khi audit lại trong lượt (2026-08-26) review commit `25fe8e4`/`788c48d`, xem log ngay dưới. Verification lúc code: `npm --prefix ui run build -- --configuration development` pass hash `62bbe370550a11ffea55`; `npm --prefix ui run test:ci` báo 113/114 (1 fail `assessmentGradeText(null)`, cho là pre-existing) — con số này **không tính 2 fail filename prefix nói trên**, tức lúc đó thực tế đã là 112/114 chứ không phải 113/114; không có build/test backend vì thay đổi chỉ ở frontend.
- `ASH-FE-11` (2026-08-26, follow-up sau audit, chưa commit tại thời điểm ghi log): vá cả 4 fail đã ghi ở bullet dưới, cộng 2 yêu cầu đổi hành vi mới từ người dùng:
  - Đổi cú pháp tên file PDF: `buildPlanPdfFileName` (`assessment-sheet-plan-preview.models.ts`) đổi signature sang `(studentCode, studentNickName, startDate, dueDate)`, sinh tên dạng `khcn - <code>.<nick>_<assessmentName>.pdf` (bỏ đoạn `.<nick>` nếu nickname trống). `assessmentName` là công thức MỚI, khác `formatAssessmentPeriod` (chữ "N tháng ..." ở header preview giữ nguyên không đổi): liệt kê các tháng từ `startDate` tới `dueDate`, chỉ tính luôn tháng của `dueDate` nếu `dueDate` là ngày cuối cùng của tháng đó, ngược lại bỏ tháng đó ra (vì `dueDate` khi không phải cuối tháng được hiểu là tháng "hạn chót" chưa xong, không phải tháng kế hoạch trọn vẹn). Khớp cả 2 ví dụ: `2026-06-01`→`2026-08-31` (hạn là cuối tháng) ra `6.7.8.26`; `2026-03-01`→`2026-06-26` (hạn giữa tháng) ra `3.4.5.26` đúng như người dùng yêu cầu.
  - Đổi fallback `assessmentGradeColor`/`assessmentGradeBgColor` (`assessment-sheets-form.component.ts`) từ `EMPTY_GRADE_OPTION.color`/`.bgcolor` sang `''`, khớp với `assessmentGradeText` đã trả `''` sẵn — theo xác nhận của người dùng đây là hành vi mong muốn (cả 3 hàm đồng nhất trả `''` khi grade null/không khớp), không phải bug cần vá theo hướng ngược lại. `EMPTY_GRADE_OPTION` vẫn giữ nguyên, chỉ còn dùng cho `gradeSelectOptions` (mục "Chưa có" trong dropdown).
  - Cập nhật lại test theo hành vi mới "khoá nút In Kế hoạch PDF khi status Open": test `allows opening the plan PDF preview...` giờ assert `canOpenPlanPdfPreview()` là `false` khi `originalStatus === 'Open'`, `true` khi `Planed`/`Done`.
  - Verification: `npm --prefix ui run test:ci` → **114/114 pass** (vá hết 4 fail, không phát sinh fail mới); `npm --prefix ui run build -- --configuration development` → pass hash `5db6c70f5becc2ddf940`, chỉ còn warning CommonJS/DevExtreme/html2pdf quen thuộc. Chưa chạy smoke thủ công cho tên file/khoá nút mới; chưa commit git tại thời điểm ghi log này. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-FE-11` (2026-08-26, commit `788c48d` "update some logic show/hide"): sửa `records-panel` trên màn edit để ẩn hẳn cột `Kết quả hiện tại`/`Ghi chú` của nó khi `originalStatus === 'Open'` (getter `showResult`, chỉ hiện khi `Planed`/`Done`), và luôn hiện cột `Kế hoạch`/`Ghi chú` của nó khi `Open` bất kể checkbox `Hiện kế hoạch` (getter `showPlan`). `canOpenPlanPdfPreview()` thêm điều kiện `originalStatus !== 'Open'` — nút `In Kế hoạch PDF` giờ bị khoá cho tới khi sheet qua khỏi `Open`. File `google-token-store/...TokenResponse-user` đổi trong cùng commit chỉ là access/refresh token OAuth được làm mới khi chạy backend, không phải thay đổi code. **Audit lại khi review commit này (2026-08-26)**: chạy lại `npm --prefix ui run build -- --configuration development` → pass hash `45d06a5a4201b59a27c9`; chạy lại `npm --prefix ui run test:ci` → **110/114 pass, 4 fail**, tất cả trong `assessment-sheets.component.spec.ts`: 1 fail mới do chính thay đổi này (`allows opening the plan PDF preview only for saved edit sheets with records` — test không set `originalStatus`, giữ default `'Open'` nên `canOpenPlanPdfPreview()` giờ trả `false`); 2 fail do đổi prefix filename PDF ở `25fe8e4` (log ngay trên) chưa từng được vá; 1 fail do `assessmentGradeText(null)` trả `''` thay vì `'Chưa có'` (thiếu fallback `EMPTY_GRADE_OPTION.text`, có từ trước cả hai commit này, không liên quan). Chưa sửa code/test nào trong lượt audit này — chỉ rà soát và cập nhật tài liệu theo yêu cầu người dùng; 4 fail trên là nợ kỹ thuật cần một agent khác xử lý (sửa test cho khớp hành vi mới hoặc sửa code nếu hành vi không như ý).
- `ASH-FE-12` (2026-08-27): tạo task mới và hoàn tất code tự động cho nút `In Kết Quả PDF`. Frontend thêm nút trên màn edit, chỉ bật khi sheet đã lưu/có record/không loading hoặc mutate và trạng thái đã lưu `originalStatus != Open`; thêm route `/#/assessment-sheets/:id/result-pdf-preview`, reuse preview A4/html2pdf của kế hoạch theo `pdfKind`, dùng `FinalGrade`/`FinalNote`, mở blob PDF bằng `html2pdf().outputPdf('bloburl')`, upload PDF do UI tạo qua `POST /api/v1/assessment-sheets/{id}/upload-result-pdf` và hiển thị `ResultFileLinkPdf`. Backend thêm endpoint upload result PDF nhận multipart `file`, validate PDF không rỗng/tối đa 10 MB, upload vào `Student.DriveFolderId`, cập nhật `ResultFileLinkPdf`, audit `AssessmentSheet.ResultPdfUploaded`, không thêm migration. Đồng thời khôi phục màu `ASSESSMENT_GROUP_LV2_CONFIGS` đúng requirement/spec cũ sau khi test phát hiện source lệch. Verification: `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass 0 warning/error; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; `dotnet test api/tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 29/29 khi chạy ngoài sandbox để truy cập integration/Docker; `npm --prefix ui run test:ci` pass 116/116; `npm --prefix ui run build -- --configuration development` pass, chỉ warning CommonJS/DevExtreme/html2pdf quen thuộc. Chưa chạy smoke thủ công màn edit/result preview và chưa kiểm tra upload Google Drive thật, nên `ASH-FE-12` giữ `[~]`. Không production/IIS/deploy.
- `ASH-CR-02`/`ASH-BE-06` (2026-08-27): tạo task mới theo yêu cầu: thêm nút `Cập nhật Kết Quả` trên màn edit để gọi `submit-results`, backend ghi `FinalGrade`/`FinalNote` vào ResultSource nếu có thay đổi và audit chi tiết từng cell được ghi; đồng thời bổ sung/chuẩn hoá audit cho `sync-assessments`, `upload-plan-pdf`, `upload-result-pdf`. Phát hiện ban đầu: ResultSource config hiện có đủ để dò/ghi cell grade, nhưng chưa thấy mapping cell/range cho `FinalNote`, nên task detail đánh dấu ❓ để không tự đoán vào Google Sheet thật. Đang triển khai, chưa chạy verification.
- `ASH-CR-02` (2026-08-27): người dùng xác nhận mapping `FinalNote`: cột note nằm ngay bên phải cột kết quả của học sinh; cột kết quả là cột có `studentCode` ở hàng định vị mã học sinh, còn ô cùng hàng của cột note để trống. Đã cập nhật task detail/status và chuyển hướng backend triển khai ghi/so sánh/audit cả grade cell lẫn note cell theo mapping này.
- `ASH-CR-02`/`ASH-BE-06` (2026-08-27): hoàn tất code và automated verification. Backend `submit-results` đọc ResultSource trước, dò cột kết quả bằng `studentCode`, dùng cột ngay bên phải làm `FinalNote` và fail nếu ô header/mã học sinh của cột note không trống; chỉ batch update các cell khác giá trị hiện tại, cập nhật `SubmissionDate`, audit tổng `AssessmentSheet.ResultsSubmitted` và audit từng cell `AssessmentSheet.ResultSourceCellUpdated`. Audit sync Google Sheet thêm `GoogleSheets.AssessmentsSynced`; upload plan/result PDF chuẩn hoá payload có fileName/fileSizeBytes/oldLink/newLink/student/sheet/date và không lưu bytes/secret. Frontend thêm nút tiếng Việt `Cập nhật Kết Quả`, confirm khi form dirty, loading state và apply lại detail sau submit. Verification: Debug build bị chặn bởi process API đang giữ DLL trong `bin/Debug` nên dùng Release gate; `dotnet build api/AdminPortal.slnx -c Release --no-restore` pass; `dotnet test api/tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; integration trong sandbox fail do không có quyền Docker pipe, chạy lại ngoài sandbox pass 30/30; `npm --prefix ui run test:ci` pass 118/118; `npm --prefix ui run build -- --configuration development` pass, chỉ warning CommonJS/DevExtreme/html2pdf/canvg quen thuộc. Chưa gọi Google Sheet/Drive thật và không chạy production/IIS/deploy.
- `ASH-CR-02` UI rule update (2026-08-27): theo yêu cầu bổ sung, nút `Cập nhật Kết Quả` chỉ hiển thị trên màn edit khi trạng thái saved của AssessmentSheet là `Done`; role `Teacher` vẫn thấy nút khi `Done` nhưng bị disable ở UI. Backend/REST contract và quyền endpoint `submit-results` không đổi, Teacher vẫn không bị chặn riêng nếu gọi API trực tiếp. Đã cập nhật requirement/plan/task detail. Verification: sandbox npm bị `EPERM lstat C:\Users\sangn`, chạy ngoài sandbox `npm --prefix ui run test:ci` pass 118/118; `npm --prefix ui run build -- --configuration development` pass hash `cda22e75995f61a01dfa`, chỉ warning CommonJS/DevExtreme/html2pdf/canvg quen thuộc. Không chạy backend vì không đổi BE/REST; không production/IIS/deploy.

- `ASH-BE-05` (2026-08-27): added anonymous local/manual Google credential smoke endpoint `GET /api/v1/google-sheets/credential-smoke`. It triggers the existing OAuth credential flow, performs a metadata-only read of configured `GoogleSheets:SpreadsheetId`, and returns safe status/metadata with coarse error codes only; it does not require portal login, return tokens/secrets, or re-add legacy `[F01]` actions. Verification: `dotnet build AdminPortal.slnx -c Release --no-restore` pass 0 warnings/errors; `dotnet test tests/AdminPortal.UnitTests -c Release --no-restore` pass 85/85; integration in sandbox failed on Docker named pipe access, then `dotnet test tests/AdminPortal.IntegrationTests -c Release --no-restore` pass 31/31 outside sandbox. Live Google OAuth/read smoke was not run; no production/IIS/deploy.

- `ASH-STT-01` (2026-08-28): sửa lỗi màn hình scroll giật lên đầu bảng khi bấm nút `Di chuyển` (spinup/spindown) trong `records-panel` màn edit AssessmentSheet. Nguyên nhân: `*ngFor="let row of recordRows"` không có `trackBy` nên mỗi lần `moveRecord()` gán lại `this.recordRows = buildAssessmentSheetRecordRows(...)` (toàn bộ row object mới), Angular huỷ và dựng lại toàn bộ `<tr>`/`dx-button`/`dx-text-box`, khiến trình duyệt reset `scrollTop` về 0. Sửa: thêm `trackRecordRow` trả `row.record.id` và `trackBy: trackRecordRow` vào `*ngFor` ở `assessment-sheets-form.component.html`, để Angular tái sử dụng DOM và chỉ đảo thứ tự hàng. Verification: `npm --prefix ui run build -- --configuration development` pass hash `4ccf5c1be20d01a85699`, chỉ warning CommonJS/DevExtreme quen thuộc. Chưa chạy `test:ci` (thay đổi chỉ là trackBy template + 1 helper thuần) và chưa smoke thủ công. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-STT-01` (2026-08-28): thêm hiệu ứng highlight cho 2 dòng vừa hoán đổi khi bấm `Di chuyển`. `moveRecord()` gọi `highlightMovedRecords(currentRecord.id, targetRecord.id)`: xoá highlight cũ rồi set `recentlyMovedRecordIds` trong `setTimeout(0)` (để Angular gỡ class khỏi DOM, animation CSS chạy lại khi bấm liên tục), tự xoá sau `MOVE_HIGHLIGHT_DURATION_MS = 1400ms`; timers được clear ở `ngOnDestroy` (component nay `implements OnInit, OnDestroy`). Template thêm `[class.record-row--moved]="isRecordRecentlyMoved(row.record.id)"` trên `<tr>`; SCSS thêm keyframe `record-row-moved-flash` (nền hổ phách `#fde68a` → trong suốt, kèm `box-shadow: inset ... 9999px` để phủ cả ô có nền riêng như textarea/`.action-cell`) trên `tr.record-row--moved > td`, có nhánh `prefers-reduced-motion` giữ nền tĩnh. Verification: `npm --prefix ui run build -- --configuration development` pass hash `150c0d64fbe088fd9227`, chỉ warning CommonJS/DevExtreme quen thuộc. `test:ci` vẫn hỏng ở bước biên dịch do lỗi có sẵn ở `assessment-sheets.component.spec.ts` (factory `editor()` thiếu field non-optional của `AssessmentSheetEditor`), không liên quan thay đổi này. Chưa smoke thủ công. Không đổi backend/REST contract, không production/IIS/deploy.
- `ASH-STT-01` (2026-08-28): tách highlight thành 2 màu theo phản hồi. Dòng người dùng chủ động bấm Di chuyển (`currentRecord`) = "primary": nháy cam đậm `#fb923c` 2 nhịp trong 1.6s; dòng bị đẩy chỗ (`targetRecord`) = "secondary": nháy vàng nhạt `#fef08a` 1 nhịp trong 1.4s. `highlightMovedRecords(primaryRecordId, secondaryRecordId)` giờ set 2 field riêng `movedPrimaryRecordId`/`movedSecondaryRecordId` (bỏ `recentlyMovedRecordIds` Set); helper `isRecordMovePrimary`/`isRecordMoveSecondary`; template dùng 2 class `record-row--moved-primary|secondary`; `MOVE_HIGHLIGHT_DURATION_MS` tăng lên 1600 để phủ animation dài nhất. Verification: `npm --prefix ui run build -- --configuration development` pass hash `bd4ca77ec85b34c33bb5`, chỉ warning CommonJS/DevExtreme quen thuộc. `test:ci` vẫn hỏng do lỗi spec có sẵn nêu trên. Chưa smoke thủ công. Không đổi backend/REST contract, không production/IIS/deploy.

## QA / smoke test log

_Chưa có hoạt động. Khi chạy `ASH-QA-01`, ghi kết quả từng bước trong 10 bước (plan mục 9 / `details/ASH-QA-01.md`) theo dạng: `- ASH-QA-01 bước N (YYYY-MM-DD): pass|fail — mô tả evidence`._

## Quyết định/điều chỉnh phạm vi

- `ASH-GRP-01` (2026-08-28): tiếp nhận yêu cầu thêm chế độ chỉnh `GroupLv2/GroupLv3` ngay trên ô merge của `records-panel`. Audit source xác nhận `AssessmentGroup` hiện chỉ là cây suy ra từ chuỗi group trên `Assessment`, không có entity/ID hoặc API mutation; snapshot nằm trong JSONB `AssessmentRecord.AssessmentSnapshot`. Tạo task detail `details/25-ASH-GRP-01.md` và đánh dấu ❓ cho phạm vi bulk update, quyền/status và tác động của `sync-assessments`. Chưa sửa source, chưa build/test.
- `ASH-GRP-01` (2026-08-28): người dùng quy luồng UI về một popup duy nhất cho từng ô merge. Popup chỉ có textbox tên mới; mặc định lưu snapshot, checkbox tùy chọn cập nhật các Assessment gốc xuất hiện trong ô, và checkbox `Ghi ngược Google Sheet` hiển thị disabled vì chưa implement backend. Bỏ thiết kế toggle chung, dropdown chuyển group và popup rename riêng. Còn chờ xác nhận quyền/status trước khi code.
- `ASH-GRP-01` (2026-08-28): người dùng chốt quyền/status: Teacher/Admin/SuperAdmin được sửa snapshot khi sheet `Open` hoặc `Planed`; chỉ Admin/SuperAdmin được chọn cập nhật Assessment gốc; sheet `Done` khóa toàn bộ thao tác. Task chuyển từ blocked sang ready, chưa sửa source và chưa chạy verification.

- (2026-08-20): Người dùng xác nhận trực tiếp áp dụng `ASH-DEC-01` (sinh PDF: ghi trực tiếp vào sheet có sẵn trong `[F01]`, export theo `gid`), `ASH-DEC-02` (mở quyền `Teacher` cho `sync-assessments` bằng role-check tại handler, không đổi policy `PortalManagers`), và `ASH-DEC-05` (khoá upsert `AssessmentSheetLatest` theo `StudentId`, `AssessmentRecordLatest` theo `AssessmentSheetLatestId` + mã mục đánh giá) đúng theo đề xuất mặc định trong plan mục 13. Đã cập nhật `status.md` (tick Status, đổi cột "Áp dụng theo" → "Đã chốt") và plan mục 13 tương ứng. Cả 3 quyết định chưa được code — chờ `ASH-BE-00`/`ASH-BE-01`/`ASH-BE-03` triển khai.
- (2026-08-25): Đính chính docs theo source hiện tại: `AssessmentRecordLatest` không còn dùng field scalar `AssessmentCode`; contract/migration hiện hành dùng FK `AssessmentId` và unique index (`AssessmentSheetLatestId`, `AssessmentId`). Các log trước đó nhắc `AssessmentCode` được giữ làm lịch sử của bản trung gian, không dùng làm chỉ dẫn triển khai mới.
